# SECURITY — 凭证管理与轮换指南

## 历史安全事件

仓库历史中曾出现生产数据库与 SSH 凭证。所有具体值、账户和生产端点均已从当前文件移除；无论旧值是否曾被标记为废弃，都必须继续按“已泄漏”处理。

当前树脱敏不等于安全事件已经闭环。以下动作必须由系统 Owner 在受控环境中确认：

1. 轮换受影响的数据库、SSH、JWT、Webhook 和其他部署凭证。
2. 验证旧凭证已失效，并保存不含秘密值的审计证据。
3. 禁用 SSH 密码登录，改用最小权限密钥和非 root 账户。
4. 评估是否协调执行 Git 历史清理；历史重写会影响所有 clone，不得由单个开发任务擅自执行。

## 凭证管理原则

1. 不得把密码、Token、API Key、私钥、内部账户或生产连接串提交到 Git。
2. 运行时凭证只通过部署平台的 secret store 或本地 gitignored 环境文件注入。
3. 示例配置只能使用明显的占位符，不得复用真实或已泄漏的值。
4. 部署与运维脚本必须通过环境变量或受控凭证代理读取秘密，不得使用 `sshpass`、明文 expect 输入或内联密码。
5. 临时凭证文件必须使用最小权限，并在 `finally`/trap 中可靠删除。

## 轮换与审计

- 数据库、SSH、SMTP 等基础设施凭证至少每 90 天轮换；发生泄漏时立即轮换。
- JWT 与服务签名密钥按安全策略轮换，并预先评估会话失效影响。
- Webhook 泄漏后立即吊销并重建。
- CI 必须扫描当前树和待合并提交中的 secrets；文档与测试 fixture 不得被宽泛 allowlist。
- 安全报告只能记录凭证类型、指纹、轮换时间和验证结论，不能记录秘密原值。

## 数据破坏脚本上锁规范

任何可能删除、清空、迁移或重建生产数据的脚本都必须同时具备：

- 显式 production opt-in；
- 精确环境与目标校验；
- dry-run 默认值；
- 人工确认或变更单关联；
- 可审计日志和回滚方案。

历史上删除的高危脚本不得恢复。确需重建能力时，应重新实现上述 guard，并经过独立安全评审。
