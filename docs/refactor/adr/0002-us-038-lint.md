# ADR-0002 · US-038 Math.random Lint Rule

**状态**：Accepted（沿用 v1.0 · v1.1 追加 SeededRandom 承接锚 · v1.2 追加 baseline 允例 Path C · v1.2.1 三方复核事实核修正 α-strict）
**签发日期**：2026-07-08 追加 · 原 v1.0 早于本轮 · Orchestrator 融合定稿 · v1.2 T+3.5 Path C 增补 · v1.2.1 T+3.5·11:52 三方 BLOCK 联署 α-strict 修正
**签发人**：Orchestrator（自签 · li-yiming msg=bf74c64c 全权授权）
**Owner**：QADocs 主
**关联**：US-038 · SeededRandom · ADR-0001 §凭证纪律并列的确定性纪律

---

## §2.1 规则

**禁**：`Math.random()` 调用全 backend + 因子引擎 + 卫星层信号计算

**允**：`SeededRandom(seed)` 显式播种伪随机数生成器（deterministic）

## §2.2 CI 层实现

- `eslint-plugin-no-math-random`（US-038 rule id）
- pre-commit hook grep `Math\.random\(\)` 全 backend TypeScript 代码
- 命中 = **red 断言**（PR reject）
- 例外白名单：`__tests__/**` 允许 · 但 CI mock random seed 保证确定性

### §2.2.1 v1.2 增补 · Baseline 允例 + 反蔓延门禁（Path C）

**背景**：Task #29 `test_no_math_random_us_038_rule.test.ts` 实机跑（SHA `19c5fed`）揭生产代码违反（QADocs escalation msg=f223ec87 首报 11 处 · 三方独立复核实测 **16 处 · v1.2.1 修正**）。

**Orchestrator 独占裁 α-strict**（msg=0a347004 + msg=165216d0 三方 BLOCK 联署终裁）：采纳 **Path C · 直修 + baseline 混合**。

**v1.2.1 事实核修正**（三方独立 grep 复核 · DataPipeline msg=71fbd3d1 · QADocs msg=e8d84a50 · Strategy msg=cc445895）：
- 命中总数：**16 处**（非 v1.2 声明的 11 处）· 排除 3 注释锚（MarketController:117 // · BayesianOptimizer:31/169 /* · MonteCarloStressTest:48 /*）后 live 生产命中
- **3 处直修红线**（不入 baseline · 永不入豁免）：
  - M-1 `backend/src/quant/backtest/internal/QuantBacktestService.ts:665` groupId label · Task #35 Strategy 承接 · `crypto.randomBytes(4).toString('hex')`
  - M-2 `backend/src/services/research/factor-discovery.ts:197` rng default · Task #36 QADocs Task #29 Path C PR 同批 burndown · SeededRandom default
  - M-3 `backend/src/services/execution/rl-execution.ts:122` rng default · Task #36 同批 · SeededRandom default
- **13 处入 baseline 存量豁免**（原 11 处 f223ec87 清单 + M-4 `backend/src/utils/redisLock.ts:59` `ID_GENERATION` + M-5 `backend/src/middlewares/uploadFeedback.ts:40` `UPLOAD_FILENAME`）

**规则增补**：
- **存量 baseline 允许**：`docs/refactor/baseline/security/us-038-baseline-<sha>.json` · SHA 锁基线冻结时点 · **v1.2.1 SHA 前移** `19c5fed` → **`a2a9300`**（双 PR merged 后基线）
- **新增全禁**：命中位 ∉ baseline → CI red 断言（PR reject）
- **只减不增**：baseline 只能通过 burndown PR 缩减 · 大小上限 = 冻结 SHA 时点条目数（**13 entries · v1.2.1 修正**）
- **schema 完整性**：每 entry 必含 `{file, line, sha256_of_line, category}` 4-tuple · 缺任一 → CI fail
- **schema 唯一性**：**联合主键 `(file, line, sha256_of_line)` 三元组去重**（DataPipeline msg=c5aa4e98 §三 揭 4 处同 sha256 碰撞位 · 单纯 sha256 会碰撞）
- **category 5 值枚举**：`ID_GENERATION` / `NONCE` / `JITTER_BACKOFF` / `UPLOAD_FILENAME` / `WEBSOCKET_CLIENTID`（v1.2 冻结 · **v1.2.1 明示不扩容** · M-1/M-2/M-3 属直修红线不入豁免通道 · 无需 `BACKTEST_GROUP_LABEL/RNG_DEFAULT_PARAM` 枚举）
- **例外白名单沿用**：`__tests__/**` 允许 · CI mock random seed 保确定性

**直修红线段（v1.2.1 新增）**：
- M-1/M-2/M-3 三路属"直修红线" · 永远不入豁免通道
- 未来任何 burndown PR 引 baseline 时 · 若触碰 `backend/src/quant/backtest/**` / `backend/src/services/research/factor-*/**` / `backend/src/services/execution/**` 三路 · **强制 Strategy owner 副签硬门禁**（联动 §PR-L §13.1 satellite 独占目录锚 · §13.4 factor discovery 候补位）
- M-3 execution 层与 li-yiming "资金链路"红线（H1-H9 live trading）相邻 · 属敏感直修位

**burndown 承接**：
- 未来 burndown PR = 单文件 replace + baseline JSON 同批减 entry · 独立 PR · QADocs 主签
- 目标 baseline size **13 → 0** · KPI 可视化（QADocs 侧 CHANGELOG 追踪）
- 替换方案标准 API：`crypto.randomUUID()` / `crypto.randomBytes(N).toString('hex')` / `SeededRandom.factory(seed).next` · 无引入新 dep

**Cross-Ref**：Task #29 承接位（本 ADR §2.4 已列）· Task #30 gitleaks baseline 同款范式（一致性）· QADocs msg=f223ec87 escalation（首报 11 处 · v1.2.1 事实核修正为 16 处）· Orchestrator msg=0a347004 §一 独占裁 · Orchestrator msg=165216d0 §二 α-strict 终裁 · ADR-0001 §附录 v1.3 事件位 · ADR-0001 §附录 v1.4 事件位（教训 (d) 声明性数值必先 grep 复核）

## §2.3 依据

- 回测确定性可复现要求 · seed 固定 → 结果一致
- 因子引擎数值稳定性要求（`Math.random()` 引入非确定性风险）
- ADR-0001 §凭证纪律 5 铁律 · 与 §凭证纪律 并列的确定性纪律

## §2.4 Cross-Ref

- QADocs Task #29 `test_no_math_random_us_038_rule.test.ts`（**硬门禁** · v1.1 追增位承接 §No-Math-Random-US-038）
- 卫星层 5-slot / 4-slot 权重计算路径 · `IntradayMomentumDetector` · `news_evidence` slot 全部走 SeededRandom
- ADR-0003 backtest 7 gates 联动（gate G4 因子稳定性验证）
- Orchestrator msg=84fa4b84 · M-Draft 挪入路径终裁

---

**End of ADR-0002 v1.2.1**
