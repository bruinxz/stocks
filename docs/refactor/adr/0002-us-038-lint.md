# ADR-0002 · US-038 Math.random Lint Rule

**状态**：Accepted（沿用 v1.0 · v1.1 追加 SeededRandom 承接锚 · v1.2 追加 baseline 允例 Path C）
**签发日期**：2026-07-08 追加 · 原 v1.0 早于本轮 · Orchestrator 融合定稿 · v1.2 T+3.5 Path C 增补
**签发人**：Orchestrator（自签 · li-yiming msg=bf74c64c 全权授权）
**Owner**：QADocs 主
**关联**：US-038 · SeededRandom · ADR-0001 §凭证纪律并列的确定性纪律

---

## §2.1 规则

**禁**：`Math.random()` 调用全 backend + 因子引擎 + 卫星层信号计算

**允**：`SeededRandom(seed)` 显式播种伪随机数生成器（deterministic）

## §2.2 CI 层实现

- `eslint-plugin-no-math-random`（US-038 rule id）
- pre-commit hook grep `Math\.random\(\)` 全 backend TypeScript 代码
- 命中 = **red 断言**（PR reject）
- 例外白名单：`__tests__/**` 允许 · 但 CI mock random seed 保证确定性

### §2.2.1 v1.2 增补 · Baseline 允例 + 反蔓延门禁（Path C）

**背景**：Task #29 `test_no_math_random_us_038_rule.test.ts` 实机跑（SHA `19c5fed`）揭 11 处**存量**生产代码违反（QADocs escalation msg=f223ec87）。11 处**无一位于回测 / 因子引擎 / satellite slot / regime 路径** · 均为 ID 生成 / nonce / jitter / 上传路径 · 与 §2.3 依据"回测确定性 + 因子稳定性"原意无冲突。

**Orchestrator 独占裁**（msg=0a347004）：采纳 **Path C** · baseline whitelist + 反蔓延门禁。

**规则增补**：
- **存量 baseline 允许**：`docs/refactor/baseline/security/us-038-baseline-<sha>.json` · SHA 锁基线冻结时点
- **新增全禁**：命中位 ∉ baseline → CI red 断言（PR reject）
- **只减不增**：baseline 只能通过 burndown PR 缩减 · 大小上限 = 冻结 SHA 时点条目数
- **schema 完整性**：每 entry 必含 `{file, line, sha256_of_line, category}` 4-tuple · 缺任一 → CI fail
- **category 5 值枚举**：`ID_GENERATION` / `NONCE` / `JITTER_BACKOFF` / `UPLOAD_FILENAME` / `WEBSOCKET_CLIENTID`（v1.2 冻结）
- **例外白名单沿用**：`__tests__/**` 允许 · CI mock random seed 保确定性

**burndown 承接**：
- 未来 burndown PR = 单文件 replace + baseline JSON 同批减 entry · 独立 PR · QADocs 主签
- 目标 baseline size 11 → 0 · KPI 可视化（QADocs 侧 CHANGELOG 追踪）
- 替换方案标准 API：`crypto.randomUUID()` / `crypto.randomBytes(N).toString('hex')` · 无引入新 dep

**Cross-Ref**：Task #29 承接位（本 ADR §2.4 已列）· Task #30 gitleaks baseline 同款范式（一致性）· QADocs msg=f223ec87 escalation · Orchestrator msg=0a347004 §一 独占裁 · ADR-0001 §附录 v1.3 事件位

## §2.3 依据

- 回测确定性可复现要求 · seed 固定 → 结果一致
- 因子引擎数值稳定性要求（`Math.random()` 引入非确定性风险）
- ADR-0001 §凭证纪律 5 铁律 · 与 §凭证纪律 并列的确定性纪律

## §2.4 Cross-Ref

- QADocs Task #29 `test_no_math_random_us_038_rule.test.ts`（**硬门禁** · v1.1 追增位承接 §No-Math-Random-US-038）
- 卫星层 5-slot / 4-slot 权重计算路径 · `IntradayMomentumDetector` · `news_evidence` slot 全部走 SeededRandom
- ADR-0003 backtest 7 gates 联动（gate G4 因子稳定性验证）
- Orchestrator msg=84fa4b84 · M-Draft 挪入路径终裁

---

**End of ADR-0002 v1.2**
