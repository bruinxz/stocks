# 信号优先重构计划 (Signal-First Plan)

> 决策档。只保留最新结论, 不留修订历史。任何决定变化时**改这份文档**, 不追加。

---

## 目标 (三层, 从最终目标到近期指标)

**大白话**: 这个项目为什么存在, 分三层看——最上面是"我到底想要什么"(真目标), 中间是"系统怎么帮我实现"(产品/系统目标), 最下面是"具体怎么摆钱"(结构目标)。越往上越是北极星, 越往下越具体。

### 层 1: 真目标 (北极星)

**在 A 股赚钱 —— 持续复利, 分两层目标**

- **底线目标 (必达): 组合年化 8-10%** —— 只靠核心 ETF 因子就能兜住, 不依赖卫星; 即使卫星被冻结/永久停也不击穿 (靠 §10.4 降级结构兜底). 30 万 -> 一年 32.4-33 万
- **冲刺目标 (stretch): 组合年化 10-15%** —— 需核心达上限 + 卫星打满才够, **不当必达**. 30 万 -> 一年 33-34.5 万
- 不追求暴富, 也不能忽赚忽亏
- **持续**是关键: 长期跑赢 = 复利, 短期爆发 + 长期回吐 = 无用
- **为什么分层** (呼应 §12.2 元教训): "梦想数字"若当成必达下限, 会绑架方案、逼高风险卫星撑大梁, 与风控纪律冲突. 底线交给核心, 冲刺交给卫星, 两者解耦

### 层 2: 产品/系统目标 (北极星怎么实现)

系统只做一件事——**告诉你什么时候该买卖什么股票**:

```
一个信号源 -> 两个出口 -> 回测验证可信度

出口 A (现在): 给用户 -> 用户拍板
出口 B (后期): 自动执行
同一套信号回灌回测, 回答"这信号可不可信"
```

**并非**"帮你跑赢基准" (JFQA 2025 证明散户中频拍板长期难赢, 详见 §3), 而是:
- **降低情绪化决策** (用户看到 gate_reason + confidence 校准数字, 减少盲目冲动)
- **系统化风控** (卫星硬边界 5%/60 天/3 个月; PR-L 熔断; 单仓上限)
- **部分捕获学术支撑的因子 alpha** (Value/Quality/LowVol, 有 JFE/JAM 论文锚定, 详见 §3)

### 层 3: 结构目标 (投资组合)

```
底线目标    = 组合年化 8-10% (核心兜底, 必达)
冲刺目标    = 组合年化 10-15% (卫星打满, stretch)
主线结构    = 核心 70% (ETF 因子) + 卫星 20% (题材事件) + 现金 10%
资金        = 10-50 万 (以 30 万为基准计算)
决策方式    = 用户拍板 (核心月度, 卫星按事件)
```

> 三层的详细锁定依据 (学术论文 + A 股实证 + 外部审计) 见 §3 最终锁定; 系统骨架与信号契约见 §2 产品骨架。

---

## -1. 术语表 (股票小白必读)

这份文档里有很多"股票黑话", 先在这里一次性讲清楚。看正文时遇到词回来查。

### -1.1 交易 / 组合基本词

| 词 | 白话解释 |
|---|---|
| **组合** | 你所有股票加起来的整体, 就是你账户里那一堆股票的总和 |
| **仓位 / 持仓** | 你手里持有的股票 (相对于全部资金而言的比例) |
| **单仓上限** | 一只股票最多能占你总资金的多少 (例: 15% 就是 30 万里最多 4.5 万买一只) |
| **建仓 / 加仓** | 买入 |
| **平仓** | 卖出全部 (清掉这只股票) |
| **减仓** | 卖出一部分 (还留一些) |
| **止损** | 亏到一定程度强制卖 (例: 亏 7% 就砍, 不让继续亏) |
| **止盈** | 涨到一定程度强制卖 (例: 涨 20% 就卖, 落袋为安) |
| **换手 / 调仓** | 卖旧的买新的 (把持仓从 A 换成 B) |
| **再平衡** | 定期检查, 把偏离目标的仓位调回目标比例 |
| **回撤** | 从最高点跌下来的幅度 (例: 100 万涨到 120 万又跌回 90 万, 回撤 = 25%) |
| **年化收益** | 一年下来赚多少% (例: 12% 就是 100 万变 112 万) |
| **Sharpe** | 收益质量分, 高 Sharpe = 收益高且波动小 (稳赚), 低 Sharpe = 忽赚忽亏 |

### -1.2 股票类型 / 工具

| 词 | 白话解释 |
|---|---|
| **A 股** | 中国大陆股票 (上海 + 深圳 + 北交所) |
| **ETF** | 一篮子股票打包, 一手买 = 同时买几十只成分股, 分散风险。像基金但能像股票一样交易 |
| **宽基 ETF** | 跟踪大盘指数的 ETF (沪深 300 / 中证 500 / 创业板), 代表整个市场 |
| **行业 ETF** | 只买某个行业的 ETF (半导体 / 医药 / 银行) |
| **主题 ETF** | 追热点的 ETF (AI / 光伏 / 新能车), 波动大 |
| **成分股** | 一个 ETF / 指数里包含的具体股票 (沪深 300 = 300 只股票, 那些股票就是成分股) |
| **T+1** | A 股规则: 今天买的明天才能卖 (锁一天) |
| **涨停 / 一字板** | A 股股价一天最多涨 10% (创业板 20%), 到顶叫涨停; "一字板" = 全天从开盘就顶格, 挂不进去买单 |
| **集合竞价** | 开盘前 9:15-9:25 集中撮合, 9:25 一次成交出开盘价 |
| **限价单 / 挂单** | 你指定"最多这价买", 到价才成交, 不到价挂着 |

### -1.3 因子 / 策略词

| 词 | 白话解释 |
|---|---|
| **因子** | 挑股票的一种规则 / 特征。学术研究证明某些因子长期跑赢市场 |
| **Value (价值)** | 挑便宜的股票 (PE / PB 低, 股息率高) —— "买打折的" |
| **Quality (质量)** | 挑赚钱能力强的股票 (ROE 高, 利润稳定) —— "买好公司" |
| **LowVol (低波)** | 挑波动小的股票 (股价不忽上忽下) —— "买稳的" |
| **Momentum (动量)** | 挑最近涨得好的股票 —— "追涨" (A 股不好用, 会追高被套) |
| **PE / PB / ROE** | 财务指标。PE = 市盈率 (股价/每股利润), PB = 市净率 (股价/每股净资产), ROE = 净资产收益率 (公司赚钱效率) |
| **z-score** | 标准化分数, 让不同量纲的因子能加权求和 (例如把 PE 和 ROE 变到同一尺度) |
| **因子轮动** | 定期按因子分排序, 挑分高的换掉分低的 (机械化选股, 排除主观情绪) |
| **主题事件 / 题材** | 短期热点炒作 (AI 火了 → 相关股一起涨, 冷了 → 一起跌) |
| **回测** | 用历史数据模拟策略, 看"如果当时这么做, 现在赚多少" |
| **实盘** | 真金白银下单 |
| **纸面 / paper trading** | 模拟盘 (下单信号进系统但不真下钱), 用来试新策略 |

### -1.4 系统词 (本项目专用)

| 词 | 白话解释 |
|---|---|
| **信号 (Signal)** | 一条"该干啥"的建议: (哪只股, 买还是卖, 什么时间, 有多信心) |
| **detector** | 生成信号的程序模块 (例如 ThemeFermentationDetector = 题材发酵探测器) |
| **管线 / pipeline** | 一条信号从生成到入库的流水线; 我们目前有 3 条并存 |
| **B-fusion 管线** | 系统里的一条信号流水线 (95 笔 0% 胜率的祸首), 融合了 20 个同类策略结果, 后面要删 |
| **confidence (置信度)** | 该信号历史胜率 (例: 该 detector 过去 90 天报的信号里, 65% 后来真的赚了 → confidence = 0.65) |
| **EV (期望值 gate)** | 期望值门槛, 只有"预期收益 > 门槛"才下单。EV = 胜率 × 平均赚 − (1-胜率) × 平均亏 |
| **主线 / 核心** | 系统主战场 (占资金 70%), 稳中求进. **"核心-卫星 (Core-Satellite)"** 是投资界经典组合结构 — 想象太阳系: 核心 = 太阳, 稳定发光, 是收益主要来源. 这里指 70% ETF 因子部分. |
| **卫星** | 围着核心转的辅战场 (占资金 20%). 比喻上是围绕太阳的行星: 数量少、波动大、受核心引力约束 (硬边界 ≤20%). 这里指 20% 题材事件驱动部分, 可能亏可能高收益, 严控上限. **"卫星累计亏损"** = 那 20% 里所有股票加起来在一段时间的总亏损. |
| **战略镜子** | 每月自我审视机制, 用 6 道题看清"这个月赚亏归因是什么" |
| **override** | 用户不听系统建议手动强行下单 |
| **regime** | 市场状态 (牛市 / 熊市 / 震荡市) |
| **cooldown 冷却期** | 触发某条件后一段时间不许再下单 (例: 一个题材刚亏了, 30 天不许再碰同题材) |
| **PR-x** | 我们做过的 PR (Pull Request, 代码改动) 编号, 例如 PR-L = 紧急停损那次改动 |

### -1.5 简写词

| 词 | 白话解释 |
|---|---|
| **α (alpha)** | 超额收益, 比基准 (通常是沪深 300) 多赚多少 |
| **prior** | 先验估计 (在没数据时先猜一个合理值) |
| **Wilson CI** | 一种统计方法, 判断"样本够不够多才可信"的置信区间 |
| **cron** | 定时任务 (例如"每天 8 点自动跑一次数据同步") |
| **backfill** | 补历史数据 (例如从 2022 年 1 月开始重新计算所有 ETF 的因子分) |

**读文档小贴士**: 遇到不懂的词先来这里查, 别硬啃。看不懂的词也可以给我提, 我加到这里。

---

## 0. 全局约束

**大白话**: 这个项目的所有"规矩"分 4 类。A 类是文档明文准则 (§0.1-§0.4, 判断代码留删 + 方案选择); B 类是工作方法 (§0.5, 怎么讨论、怎么维护文档); C 类是产品/技术约束 (§0.6, 赚钱目标、合规、风控); D 类是交互约束 (§0.7, 做事方式)。动手前先扫一遍这 4 类。

### 0.1 正确性优先准则

**大白话**: 判断一段代码要不要留下, 看两件事——**思路对不对** 和 **有没有在用**。

判断代码留 / 删用二维矩阵 (思路对错 × 用没用):

| 思路 | 在用 | 处置 |
|---|---|---|
| 正确 | 在用 | 保留 |
| 正确 | 没在用 | 可以删 (留着是 noise) |
| 错误 | 在用 | 改思路或重写 |
| 错误 | 没在用 | 删 |

"干净" = 思路正确合理 + 有在用。
代码多 ≠ 脏, 烂思路才是脏。

**优先级**: 正确 > 快, 思路对 > 代码量小, 不图快图对。

### 0.2 例外 (必须保留, 即便思路待评)

**大白话**: 即使思路暂时讲不清, 有两类东西也不能动——一是**保命的**, 二是**大家都要用的公共设施**。

- PR-L 紧急停损 (实盘 win% 转正前) —— 相当于紧急刹车, 有 bug 也得留着别拆
- 公共基础设施 (DB / cron 框架 / 日志 / monitoring) —— 全系统都依赖, 动一个死一片

### 0.3 决策前置

**大白话**: 想写新代码前, 先在这份文档里说清楚"我为啥要写这个", 得到自己 / AI 确认再动手。防止边写边改思路造成的浪费。

- 决定要写代码前, 先在文档中写出该模块 "思路对不对" 的判断, 通过后再执行

### 0.4 方案选择准则 (规范 > 快)

**大白话**: 遇到方案选择时, AI 应默认选**更规范 / 更长期 / 更有扩展性**的那一个, 而不是"哪个更简单快"。今天省 30 分钟, 半年后拆重构要花 3 天, 是净亏。

**判断顺序**:
1. **规范优先**: 有行业标准 (e.g. Sequelize migration / soft delete / composite index) 就用标准, 不自己发明
2. **长期优先**: 6 个月后 (数据量 10x / 用户 override 500 次 / 卫星永久停) 该方案能不能扛住
3. **扩展性优先**: 未来加维度 (加因子 / 加 detector / 加 regime 类型) 时改动是"加字段"还是"重建表"
4. **只有当以上三项打平**才比较开发成本

**例子**:
- ID 用 `<type>-<key>-<yyyymmddhhmm>` 而不是 `nanoid()` → 更规范, 人类可读, 便于排查 (规范 > 快)
- BUY-SELL 每次配对**新建独立 lifecycle_id**, 而不是"多次加仓复用同一 id + lot_seq" → 每笔独立更容易扩展"分批止盈"、"部分平仓" 而不用改 schema (扩展性 > 简洁)
- DB 存 timestamp 用 `TIMESTAMPTZ` 不用 `TIMESTAMP` → 时区问题一次到位

**反例 (禁止)**:
- "现在只有一个 admin, 权限不用做" → 未来加人时权限散在每个 controller, 重构成本极高
- "缓存 15 分钟, 数据不新鲜没关系" → 未来实盘触发时靠这个数据决策, 停在 15 分钟前的错数据一次亏几万

### 0.5 工作方法约束 (B 类)

**大白话**: 这些是"怎么一起把项目聊清楚、怎么维护这份文档"的规矩。

| # | 约束 | 来源 |
|---|---|---|
| B-1 | **只保留最新结论, 不留修订记录**: 任何决定变化直接改这份文档, 不追加历史 | 顶注 + 会话约定 |
| B-2 | **聚焦项目本身**, 不围绕个人展开 | 会话约定 |
| B-3 | **一个一个聊, 直到把项目聊清楚**, 由 AI 挑选讨论方向 | 会话约定 |
| B-4 | **AI 主动挑战, 不做确认偏差**: 默认会接住用户方向, 需被明确要求才反驳 | §12.1 |
| B-5 | **锁定大决策前, 用 sub-agent 模拟"苛刻外部审计员"挑刺** | §12.1 |
| B-6 | **后续自动部署**, 不必每次询问 | 会话约定 |
| B-7 | **文档保持干净**: 不留审计账本、不加"审计 X 采纳"标签, 每处修改都像原生设计 | 会话约定 |
| B-8 | **数字禁止拍脑袋**: 新数字必须锚定到 §11.1 (已锚定) 或 §11.2 (待校准) 之一 | §11.3 |
| B-9 | **术语黑话 + 白话双解**: 遇到不懂的词随时补进 §-1 术语表 | §-1 |

### 0.6 产品/技术约束 (C 类)

**大白话**: 这些是"系统往哪走、能用什么、不能用什么"的硬约束。

| # | 约束 | 来源 |
|---|---|---|
| C-1 | **真目标是赚钱**: 底线年化 8-10% (核心兜底, 必达) / 冲刺 10-15% (卫星打满, stretch), 持续 (不要短期高然后回吐) | §3 / 目标层 1 |
| C-2 | **立场: 换, 但要证据**: 有硬证据推翻锁定才换 | §3 |
| C-3 | **决策方式 = 用户拍板** (非全自动): 系统不宣称跑赢基准, 定位为"降低情绪化 + 系统化风控 + 部分捕获因子 alpha" | §3 (JFQA 2025) |
| C-4 | **数据源合规**: 免费方案优先; 排除爬虫 (雪球/东财) 与付费终端 (Wind/iFinD) | §6.1 |
| C-5 | **PR-L 紧急停损保留**: 实盘 win% 转正前不许拆 | §0.2 / §5.4 |
| C-6 | **一切决策可回滚, <15 分钟**: B-fusion 删除前必须先影子对比 30 天 | §13.1 |
| C-7 | **不发手机 push** (加剧情绪化), **允许飞书消息**; PR-L / 硬边界告警永远发 | §13.2-§13.3 |

### 0.7 交互约束 (D 类)

**大白话**: 这些是"具体做事方式"上的偏好。

| # | 约束 |
|---|---|
| D-1 | **别暂停**: 继续讨论直到聊完, AI 不主动建议"这里可以停" |
| D-2 | **文档协作用评论、不直接编辑**: 用类似飞书的评论机制, 避免多人编辑冲突 |
| D-4 | **页面空间尽量利用**: 能利用起来就利用、单独边框可去掉、支持收起 |
| D-5 | **内容通俗易懂**: 股票小白也能看懂, 术语必须解释 |

---

## 1. 起因

**大白话**: 6 周做了一大堆事, 但两件事同时成立——**系统在亏钱**, 同时**还在不停加新东西**。这不对, 得先停下来看清楚问题在哪。

系统在 6 周内积累 30+ PR, 6 条战线并行 (数据 / detector / UI / 通知 / 风控 / bug fix)。
两个矛盾事实:

| 事实 | 数据 |
|---|---|
| 系统在亏钱 | 实盘 95 笔平仓 win% = 0%, avg -8.04% (95 单全亏, 平均一单亏 8%) |
| 信号在被加 | 6 周新增 13+ detector / 122 战法 / 6 条战线并行 |

深度诊断发现:
- prod 信号表**实际只 3 个 source_type 在写** (虽然名义上有 13 个 detector, 但真正往数据库写信号的只有 3 个模块)
- **95 笔 0% 实盘全来自 `QuantFusionService`** (B-fusion 管线), 该管线用 20 个同质化趋势策略做"伪共识" (20 个策略都是"追涨"套路, 它们全说"买" 其实只是一种意见, 不是 20 种)
- **confidence 是规则封顶** (fusion_score ≥ 96 几乎必然, 失去概率含义) —— 系统显示"96% 置信", 实际是 20 个策略投满票时封顶数值, 跟真实胜率无关
- **A 股学术证据**: 题材短反转 > 动量 (追热点通常追在顶部, 冷门反而反弹), 主题 ETF 追涨见顶后长期难恢复 (5 只 A 股主题基金实证, 详见 §3 证据锚定; 跨市场辅证见 Ben-David et al. RFS 2023 美股 specialized ETF), 散户系统性亏损

---

## 2. 产品骨架

### 2.1 骨架

**大白话**: 系统只做一件事——**告诉你什么时候该买卖什么股票**。这个建议现在给你人自己判断, 以后可以让系统自动执行。同时用历史数据回测, 检查"这些建议靠谱不"。

> **一个信号源 → 两个出口 → 回测验证可信度**
>
> 出口 A (现在): 给用户, 用户拍板
> 出口 B (后期): 自动执行
> 同一套信号回灌回测, 回答"这信号可不可信"

隐含契约 (**修正**, 原表达"信号=回测=实盘"太强):

> **同一 signal 契约 + 同一执行假设 + 同一归因口径; 回测/纸面/实盘偏差可解释、可度量、可收敛.**

翻译成大白话: 三者不追求绝对等号 (A 股涨跌停 / 集合竞价 / 滑点 / ETF 折溢价 / 数据修订 / 政策结构断点决定了实盘永远和回测有偏差), 但要求:
1. **同契约** — 三者读同一份 Signal 结构 (§2.2), 不能纸面下单用一套字段实盘用另一套
2. **同执行假设** — 回测中的成本、滑点、成交时点必须模拟实盘的执行规则 (§4.1 集合竞价改 9:40 VWAP)
3. **同归因口径** — 每笔 PnL 分解 = 信号 alpha + 执行滑点 + 折溢价 + 成本, 三处用同一分解表
4. **偏差可度量** — 每周输出 `live-vs-backtest drift report`, 包含 slippage attribution
5. **偏差可收敛** — 若 drift 单向持续 > 30 天, 触发 §10 降级 review

目前不成立 (回测 48% 胜率, 实盘 0% 胜率, 说明回测在骗人)。让偏差**可解释可收敛**是北极星。

### 2.2 信号原子定义

**大白话**: 每条"该买/该卖"的建议长成下面这个数据结构。5 个核心字段是必填 (谁, 买还是卖, 什么时间, 多信心, 谁给的建议), 其他字段自动化系统会补。

```typescript
type Signal = {
  // 核心 5 字段
  symbol: string;                              // 股票代码, 例如 "600519" (茅台)
  action: 'BUY' | 'SELL' | 'TARGET_WEIGHT';    // 买 / 卖 / 调到目标比例 (ETF 用)
  timestamp: Date;                             // 什么时候发出的
  confidence: number;                          // 该 detector 历史胜率, 0-1 之间
  source_detector: string;                     // 谁给的建议 (方便追责)

  // 生命周期
  lifecycle_id?: string;       // 用来把"买这只"和后来"卖这只"配对起来
  theme_id?: string;           // 卫星题材专用: 属于哪个题材 (AI / 光伏)
  rebalance_id?: string;       // 核心 ETF 每月再平衡时给的组标识
  target_pct?: number;         // TARGET_WEIGHT 时: 目标仓位%

  // 自动化字段 (自动化层填充)
  expected_value?: number;              // 预期收益 (EV, 用胜率×平均赚算出来)
  recommended_size_pct?: number;        // 建议买多大 (占总资金%)
  entry_price_strategy?: 'auction_open' | 'observe_15min' | 'skip';  // 咋买: 开盘直接买 / 观察 15 分钟 / 别买
  stop_loss_pct?: number;               // 跌到这个%就止损
  take_profit_pct?: number;             // 涨到这个%就止盈
  cooldown_until?: Date;                // 冷却到什么时候
  gate_pass?: boolean;                  // 通没通过 EV 门槛
  gate_reason?: string;                 // 通过 / 拒绝的原因文字说明
}
```

**关键设计**:
- **confidence 是概率, 不是得分** (之前系统里是"打分封顶", 现在必须是"过去 90 天真实胜率")
- **source_detector 让每条信号可追责** (哪个模块建议错了看得到)
- **SELL 是配对平仓, 不是看跌扫描** (系统不会主动扫全市场找"该卖空的", SELL 只针对"我已经持有的" 说什么时候卖)
- **自动化和用户拍板消费同一套信号 + 同一套 gate** (人机同源, 不搞两套逻辑)

### 2.3 3 个 ID 的规范

**大白话**: Signal 结构里有 3 个 ID 字段, 分别用来把相关信号关联起来。下面定义"谁生成 / 什么时候清 / 数据库怎么存"。

**通用格式约束**:
- 全部用**人类可读结构** `<type>-<key>-<yyyymmdd[hhmm]>`, 不用随机 UUID / nanoid
- 便于日志排查、数据库直查 (`WHERE lifecycle_id LIKE 'theme-600519-%'`)
- 遵循 §0.4 "规范 > 快"

#### lifecycle_id — BUY-SELL 配对

**用途**: 把一次进场 (BUY) 到出场 (SELL) 绑定, 方便算完整 PnL

| 维度 | 规则 |
|---|---|
| 生成时机 | BUY 信号发出瞬间 (SignalRouterService) |
| 格式 | `<detector>-<symbol>-<yyyymmddhhmm>`, 例: `theme-600519-202607011425` |
| 加仓怎么办 | **每次 BUY 都是新的 lifecycle_id** (独立配对). 不复用+lot_seq. 简化统计, 未来"分批止盈"、"部分平仓" 都直接每次新 id 而无需改 schema (§0.4 扩展性优先) |
| SELL 配对 | ①同 detector 主动 SELL: 复用最近未平仓 BUY 的 lifecycle_id; ②止损/止盈/时间到期 SELL: 系统查最近未平仓 BUY 反填 |
| SELL 找不到 BUY | 不静默丢弃 → 记入 `orphan_sells` 表, 每日 review 人工兜底 |
| 清除 | 永不清除 (统计需要历史) |
| DB 索引 | `CREATE INDEX idx_signals_lifecycle ON ai_investment_signals (lifecycle_id)` + `recommendation_trade_outcomes` 冗余同步一份用于回填 |

#### rebalance_id — 月度再平衡组标识

**用途**: 一次月度再平衡触发的多条 signal (卖 A / 买 B / 买 C) 绑同一组, 便于把这一批 PnL 汇总看再平衡效果

| 维度 | 规则 |
|---|---|
| 生成时机 | `ETFRotationService` 月度触发时生成一次 |
| 格式 | `rebalance-YYYY-MM`, 例: `rebalance-2026-07` |
| 一批共享 | 同一次 rebalance 触发的**所有** signal 用同一 id |
| 同月二次触发 | 紧急再平衡时加序号 `rebalance-2026-07-2` (少见 case) |
| 清除 | 永不清除 |
| DB 索引 | `CREATE INDEX idx_signals_rebalance ON ai_investment_signals (rebalance_id) WHERE rebalance_id IS NOT NULL` (部分索引省空间) |

#### theme_id — 卫星题材标识

**用途**: 一个题材 (AI / 光伏) 可能触发多只题材股信号, 把它们关联起来才能算"AI 这一波总共赚了多少"

| 维度 | 规则 |
|---|---|
| 生成时机 | `ThemeFermentationDetector` 探测到"启动"阶段时 |
| 格式 | `<industry_slug>-<launch_date>`, 例: `ai-computing-20260615` |
| 多只股共享 | 同题材下所有相关个股 signal 复用同一 theme_id |
| 题材重启动 | 若已退潮的题材再度启动, 生成**新版本** `ai-computing-20260615-v2`, 旧 id 保留 (历史 PnL 不动) |
| 冷却期锚定 | 题材冷却期以最新 theme_id 为准, 旧版本不计 |
| 清除 | 永不清除, 但 "退潮"后不再复用于新 BUY |
| DB 索引 | `CREATE INDEX idx_signals_theme ON ai_investment_signals (theme_id) WHERE theme_id IS NOT NULL` |

**Migration 顺序** (加字段两阶段, 遵循 §13.1):
1. 加字段, NULL 允许, 应用层双写
2. 观察 7 天数据一致, 加索引
3. 30 天后加 NOT NULL 约束 (对 BUY signal 而言 lifecycle_id 应非空)

---

## 3. 最终锁定

**大白话**: 经过多轮讨论 + 学术证据 + 外部审计, 最终定下来是这么一个组合结构:

```
底线目标    = 组合年化 8-10% (核心兜底, 必达)
冲刺目标    = 组合年化 10-15% (卫星打满, stretch, 不当必达)
                → 说人话: 30 万一年底线赚 2.4-3 万, 打满能到 3-4.5 万, 别指望暴富, 也别忽赚忽亏

主线结构    = 核心 70% (ETF 因子) + 卫星 20% (题材事件) + 现金 10%
                → 70% 稳稳赚, 20% 博高收益 (可能亏但控住), 10% 应急/收利息

资金        = 10-50 万 (以 30 万为基准计算)
决策方式    = 用户拍板 (核心每月看一次, 卫星按事件看)

单仓上限    = ETF 15% / 题材股 5% (卫星更严, 因为个股崩得快)
最大持仓    = 4-6 只 ETF + 3-4 只题材股 (总 7-10 只)
                → ETF top4 买/top6 卖 的缓冲带 → 稳态 4-6 只
                → 太多管不过来, 太少不分散

override    = 允许, 但走审计流程 (二次确认 + 写理由 + 入库)
                → 系统说"别买"你还是要买, 得写理由存档, 亏了责任在自己
```

数学核算 (30 万, 对应两层目标):
```
乐观: 12.6% 组合年化 (核心 12% + 卫星 20%)  → 30 万 → 33.8 万   [够到冲刺目标 10-15%]
中性: 8.2%  组合年化 (核心 10% + 卫星 5%)   → 30 万 → 32.5 万   [落在底线目标 8-10%]
悲观: 4.8%  组合年化 (核心 8%  + 卫星 -5%, 硬边界触发前会停) → 30 万 → 31.4 万   [破底线, 触发 §10 降级 review]
```

**为什么底线定 8-10% 而非 10-15%**: 上表中性情景 (核心 10% + 卫星 5%) 只有 8.2%, 低于 10%; 只有乐观情景 (核心、卫星双双打满) 才够 10% 下限. 将 10-15% 当必达下限, 等于逼“随时可能被冻结/永久停”的卫星稳定贡献 7-14%, 与它“探索沙盒”定位矛盾. 故底线只靠核心 (学术支撑稳) 兜住, 卫星只负责往冲刺目标推.

**证据锚定** (每个决策的实证/学术支撑):

**Value 0.40 主权重** — Liu, Stambaugh, Yuan (JFE 2019) "Size and Value in China":
- A 股 **EP (earnings-price) 因子 subsume BM (book-to-market)**, 是 Value 的主导信号
- 直接复制 Fama-French (1993) 在 A 股留下 **17% 年化 alpha 无法解释** → 强烈支持 Value 权重占核心
- CH-3 (市值 + EP) 模型覆盖大多数 A 股 anomaly (含盈利、波动率异象)
- **限制**: 结论依赖排除最小 30% shell 股票 (A 股 IPO 壳价值); reversal/短期动量需 CH-4 (加 sentiment/turnover)

**LowVol 0.30 权重** — Blitz, Hanauer, van Vliet (JAM 2021) "The Volatility Effect in China":
- A 股 **低风险异象由波动率驱动, 不是 beta**
- VOL 因子无法被 Fama-French-Carhart 解释, 独立表现在 size/value/momentum/VOL 中最强
- **限制**: (a) "低波股票显著跑赢高波股票" 断言在文献中有分歧 (对抗验证 2-1 驳回), 保留的是较窄断言 (VOL 因子有 alpha, 但不能升级为"低波必然跑赢"); (b) 作者是 Robeco 低波产品线研究员, 存在轻度利益冲突

**Momentum 0.0 shadow only** — Barroso & Santa-Clara (JFE 2015) "Momentum Has Its Moments" + Hsu 2018 A 股实证:
- Momentum 在所有主流因子中有**最严重的 crash 与显著负偏态**
- 基于实现波动率的 risk-scaled momentum 可 virtually eliminate crashes 并 near-double Sharpe
- **关键限制**: Barroso 2015 样本是**美股 1927-2011**, 不是 A 股; A 股 Hsu 2018 及后续文献显示 momentum 常不显著甚至反转
- **决策**: Momentum shadow only (权重 0.0), 若未来打开需**用 A 股独立回测重新验证**, 不能直接搬美股结论

**"卫星 20% + 5% 硬边界"** — A 股主题公募/ETF 追涨见顶实证 (5 只基金一致 pattern):

| 基金/ETF | 顶点年份 | 顶点回报 | 次年 / 后续 | 长期后果 |
|---|---|---|---|---|
| 诺安成长 320007 (蔡嵩松) | 2020-2021 | (半导体主题) | 2022 **-40.04%**, 2018 -31.83% | 蔡嵩松任期累计 -15.90% |
| 前海开源公用事业 005669 (崔宸龙, 实际重仓锂电/光伏) | 2021 | **+119.42%** (品类第 1) | 2022 -26.02%, 2023 -13.67% | 近 3 年仅 +20.59%, 2021 顶后至 2026 未恢复 |
| 光伏 ETF 华泰柏瑞 515790 | 2021 | **+50.24%** (品类 3/1822) | 近 3 年 -17.84% vs 沪深 300 +29.06% | 跑输基准 47 pct, 成立 5.5 年累计仅 +2.19% |
| 新能源车 ETF 华夏 515030 | 2021 | +41.96% | 2022 **-29.03%**, 2023 **-28.82%** | 连续 2 年双位数回撤 |
| 半导体 ETF 国联安 512480 | 成立以来 | +486.36% | 2022 单年 **-36.78%** (同期沪深 300 -21.63%) | 单年回撤是宽基的 1.7 倍 |

**Pattern**: 一致展现"追涨→顶点 +50~119%→次年 -26~40%→3-5 年长期不能回到峰值"→ **强烈支持卫星 ≤20% + 单笔 5% + 60 天累计亏损 5% 硬边界**.

来源: 东方财富基金档案 (fund.eastmoney.com/{fund_code}.html), 数据窗口截至 2026-07-01.

**主题 ETF 首 5 年跑输的跨市场辅证** — Ben-David, Franzoni, Kim, Moussawi (RFS 2023) "Competition for Attention in the ETF Space":
- **美股 specialized ETF 首 5 年 risk-adjusted 亏损约 30%** (~6-7% 年化 alpha shortfall)
- **原因**: 发行时标的股票已过度乐观定价 (extrapolative beliefs + 供给方蹭热点), **非高费率或对冲需求**
- **⚠️ 重要限制**: 这是**美股 ETF 实证**, 不是 A 股. 之前文档口头引用"主题 ETF 5 年跑输基准 30%" 严格来说是套用美股结论, **需要 Wave 2 用 A 股主题 ETF 独立回测确认**——但结合上表 5 只 A 股基金一致 pattern, 方向性可信

**stance (立场)**: 换, 但要证据. 今天已兑现一次 (从原来"题材主打"→ "ETF + 题材混合"). Value/Quality/LowVol 有充分学术支撑, Momentum 保守 shadow, 卫星硬边界有强 A 股实证.

**"用户拍板"决策的实证锚定** — Jones, Shi, Zhang & Zhang (JFQA 2025) "Retail Trading and Return Predictability in China":
- 上交所**账户级微观数据** (顶级金融期刊, FT-50), 散户按账户规模分成 5 组
- **小账户散户**: 完全没有选股能力, 追涨杀跌、无视公告、过度自信 + 博彩倾向
- **大账户散户**: 虽有选股 alpha 和反向操作能力, 但**被交易成本完全吞掉**, 净收益 ≈ 0
- **→ 直接反驳**"散户中频拍板长期跑赢基准"的乐观说法
- **→ 修正产品定位**: 系统不宣称"帮用户跑赢沪深 300", 而是"降低情绪化决策 + 系统化风控 + 部分捕获学术支撑的因子 alpha"

**监管型黑天鹅的量化案例** (支持 PR-L 紧急停损保留):

| 事件 | 日期 | 量化冲击 | 来源 |
|---|---|---|---|
| 教育双减 | 2021-07-24 | 新东方裁员 6 万、K12 段营收 -80%; 高途从 149 美元跌到 2 美元区间, -98% | 俞敏洪 2022-01 内部年会 (Reuters/SCMP/彭博), Wikipedia 转引 |
| 游戏版号 2018 冻结 | 2018-03 → 2018-12 | 版号数从 9600 → 1980; 腾讯 2018-08-15 十余年首次利润下滑, 当日港股 -3% 早盘, 该周市值蒸发 500 亿美元, 2018-10 累计 -40% / -2300 亿美元, 跌出全球前 10 | Wikipedia 转引 caixinglobal + Reuters |
| 游戏版号 2021 冻结 | 2021-08 → 2022-04 (8 个月) | 2021-08-03 "精神鸦片"报道当日腾讯港股盘中 -10%, 网易盘中 -15%; 冻结期间 14000+ 游戏公司注销 | Wikipedia 转引 SCMP + CNBC |
| 网游新规草案 | 2023-12-22 | 单日腾讯市值蒸发 460 亿美元, 5 天后政府表态改进后回升 5% | Wikipedia 转引 Reuters/Al Jazeera |
| 光伏产能过剩 | 2023-2025 | 组件价 2023 -50%, 2024 -25%; 5 家龙头 (通威 / TCL 中环 / 隆基 / 晶澳 / 爱旭) 2025 年累计预亏 289 亿人民币 | NYT (Bradsher 2024-07) + 21 世纪经济报道 2026-01-19 |

**Pattern**: 政策监管型黑天鹅 = **无预警 + 单日/单周 -10~15% 冲击** → 技术面/情绪面模型**无法预测** → **必须靠紧急停损 (PR-L §5.4) + 卫星硬边界 (§4.2) 硬规避**.

**散户行为偏差的额外提醒** — 上述 JFQA 2025 显示小账户主流行为是"追涨杀跌 + 无视公告":
- **→ 影响推荐系统设计**: 情绪面 / 龙虎榜 / 短线动量类信号需谨慎, 容易复制散户 momentum-trap
- **→ 支持 §5.2 EV gate 存在**: 卫星卖信号必须过 EV gate, 不能盲目跟 detector 输出
- **→ 支持 confidence Wilson 下界 (§5.1)**: 用样本充足 + 概率校准的方式抵消"过度自信"

**证据链缺口**:
- 大 V ≥3 年审计跟踪跑赢沪深 300 的公开数据: **未找到可靠公开源** (雪球/集思录/知乎自报数据不能作为严肃证据, 幸存者偏差极强)
- 上交所 Fact Book 2020-2024 版本官方索引缺失, A 股散户换手率原始年报数据链断裂
- 主题事件驱动**私募**基金公开净值 (私募排排网/华宝证券): 本轮未通过对抗验证, 待补证
- risk-managed momentum 在 A 股月度数据的独立回测: 待 Wave 2 实测

---

## 4. 主线设计

### 4.1 核心 70% — ETF 因子轮动

**大白话**: 主战场用**因子轮动 ETF**——每个月按 4 个因子 (估值 / 质量 / 低波 / 动量) 给几十只 ETF 打分, 挑分最高的买入 (top4 买 / top6 卖 缓冲带, 稳态持有 4-6 只), 分数掉出前 6 名的卖掉。这是机械化、无情绪的方法, 学术研究证明长期能跑赢市场。

**⚠️ 证据迁移警告**: A 股股票因子有效 ≠ ETF 能稳定捕获同等 alpha. ETF 会受**跟踪误差、成分股调整、规模流动性、折溢价、开盘价差、上市时间、幸存者偏差**影响. 因此:
- **回测数据要求** (P0 硬约束):
  - 用真实可交易 ETF 历史价格 (含分红调整), 禁止用今天的 ETF 池反推历史
  - Point-in-time 成分股/财务数据 (2023-03 的因子分只能用 2023-03 当时可得的成分和财报)
  - 真实交易成本 (手续费 0.13%×2 + 冲击成本 0.1-0.3%/8)
  - 剔除上市不足 6 个月、日均成交额 <2000 万的 ETF (流动性 gate)
  - 保留已退市/合并 ETF 的数据段 (避免幸存者偏差)
- **验收指标** (Wave 2 达标才允许 Wave 3 上线):
  - 成本后年化 ≥ 10%
  - 最大回撤 ≤ 25%
  - 换手率 ≤ 200%/年
  - 成本翻倍压力测试后 alpha 仍 > 0

**参考来源**:
- S&P Global, Examining Factor Strategies in China's A-Share Market
- MSCI, Are You Really Capturing the Right Factors? China A-Share Factor Investing

**候选 ETF 池 (46-63 只)**:

| 组 | 数量 | 内容 |
|---|---|---|
| 宽基 | 8-12 | 300 / 500 / 1000 / 创业板 / 科创 50 / 深证 100 / 上证 50 (跟踪大盘的) |
| 风格因子 | 6-10 | 红利 / 中证红利 / 高股息 / 300 价值 / 低波红利 / 500 质量 (已经做好因子筛选的 ETF) |
| 行业 | 20-25 | 消费/医药/科技/金融/军工/新能源/半导体/银行/券商/有色 (单行业 ETF) |
| 主题 | 8-10 | AI/芯片/5G/新能车/光伏/医疗器械 (慎用, 用于测试动量因子) |
| 债券/商品 | 4-6 | 国债/短融/黄金 (对冲用 + 闲置现金去处) |

**因子权重 V0** (V0 candidate, Momentum 首版降级 shadow):

| 因子 | 权重 V0 | 状态 | 计算 |
|---|---|---|---|
| Value (估值) | **0.40** | 主 | PB 倒数 + PE-TTM 倒数 + 股息率, z-score 加权 |
| Quality (质量) | **0.30** | 主 | 成分股平均 ROE + 利润增长稳定性 (5 年利润波动小) |
| LowVol (低波动) | **0.30** | 主 | 过去 60 日日收益波动 × (-1) 与 20 日波动 × (-1) 加权 |
| Momentum (动量) | **0.0 (shadow)** | 观察, 不入实盘权重 | 过去 20 日累计收益, **仅记录并 walk-forward 验证** |

**关键修订**:
- Momentum 从 0.10 降到 **0.0 (shadow only)** — Hsu et al. (2017) 论文明确显示 A 股 short-term momentum 可能反转; 20 日动量与题材卫星热点逻辑重叠, 一旦上核心会把主线拖向短线化
- Value 从 0.35 → 0.40, LowVol 从 0.25 → 0.30 — 吸收 Momentum 的 10% 权重, 加权到已验证的两个因子 (MSCI 2025 指出 A 股高股息 / 低波比全球更突出)
- **权重是 V0 candidate**, 不是已验证结论. 允许在**保守权重网格** {(0.4/0.3/0.3), (0.35/0.35/0.3), (0.5/0.25/0.25)} 上做敏感性验证, **禁止追求最优参数** (会过拟合)
- 输出: 每组权重的**换手率、最差年份表现、成本后 alpha**, 若敏感性差异 < 2% 年化则采用最保守组

**参考来源**:
- Hsu, Viswanathan, Wang, Wool (2017), Anomalies in Chinese A-Shares (SSRN 2955144)
- MSCI 2025 China A-Share Factor Investing
- CAIA, Factor Investing in the China A-Share Market

综合分 = 各因子 z-score × 权重, 加起来 (Momentum 单独存 shadow 分, 不参与排名)

**因子具体计算公式 (Wave 1 开发口径)**:

大白话: 每只 ETF 的每个因子分, 都是**"先把 ETF 展开成成分股 → 每个成分股算这个因子的原始值 → 加权平均得到 ETF 的原始值 → 在 ETF 池内做 z-score 标准化"**. 权重用成分股在指数中的权重 (`index_components.weight`).

**通用: ETF → 成分股展开**
```sql
-- 宽基/风格因子/行业 ETF: 用 index_components (跟踪指数)
SELECT stock_code, weight
FROM index_components
WHERE index_code = <etf_tracked_index>       -- e.g. '000300.SH' (沪深300)
  AND trade_date = <point_in_time_month>     -- point-in-time, 用当月末快照
```

**Fallback**: 若 `index_components` 无该 ETF 数据, 使用 `fund_top_holdings` (基金前十) — 覆盖 60-80% 权重 已足够代表. 若两个都空, 该 ETF 从当月 candidate 池剔除.

**因子 1: Value (估值, 权重 0.40)**

- 目的: 挑便宜的 ETF (PB 低、PE 低、股息率高)
- 原始值 (成分股层):
  ```
  stock_value_raw = z(1/pb) + z(1/pe_ttm) + z(dividend_yield)
  ```
  - `1/pb` = 市净率倒数 (账面价值 / 市值). 越高 = 越便宜
  - `1/pe_ttm` = 市盈率倒数. 越高 = 盈利越好
  - `dividend_yield` = 股息率. 越高 = 分红越多
  - **z-score in universe** = 在**全部候选 ETF 的所有成分股**里做横截面 z-score (不是 ETF 内部)
- 数据源:
  ```sql
  SELECT db.stock_id, db.pe, db.pb,
         COALESCE(fr.dividend_yield, 0) AS dividend_yield
  FROM daily_bars db
  LEFT JOIN financial_reports fr ON fr.stock_code = db.stock.symbol
    AND fr.report_date = latest_before(<factor_date>)
  WHERE db.time = <factor_date>              -- 月末最后交易日
  ```
- ETF 层聚合:
  ```
  etf_value_raw = Σ (weight_i × stock_value_raw_i) / Σ weight_i
  ```
- 缺失处理: 单成分股某字段缺 → 该字段用 universe median 填充; 若 > 30% 成分股缺关键字段 → 该 ETF 当月标 `data_incomplete`, 不参与排名

**因子 2: Quality (质量, 权重 0.30)**

- 目的: 挑好公司 (ROE 高, 利润稳)
- 原始值 (成分股层):
  ```
  stock_quality_raw = z(roe) + z(-stddev_5y_net_profit) + z(roe_5y_avg)
  ```
  - `roe` = 净资产收益率 (最近报告期)
  - `stddev_5y_net_profit` = 过去 5 年净利润的标准差 (取负 → 越小越好, 稳定的高分)
  - `roe_5y_avg` = 过去 5 年 ROE 均值 (吸收当期噪音)
- 数据源:
  ```sql
  SELECT sff.symbol, sff.roe, sff.net_profit_growth
  FROM stock_fundamental_factors sff
  WHERE sff.factor_date = <factor_date_point_in_time>
    AND sff.report_period = latest_annual_before(<factor_date>)

  -- 5 年 ROE 序列
  SELECT stock_code, roe, report_date
  FROM financial_reports
  WHERE report_type = 'annual'
    AND stock_code IN (<etf_constituents>)
    AND report_date BETWEEN (factor_date - 5 years) AND factor_date
  ```
- ETF 层聚合: 与 Value 同 (成分权重加权)
- Point-in-time 约束: 财报有滞后, 3 月末算 4 月的 Quality 因子必须用**已披露**的年报 (通常上一年的年报 4 月底才全披露), 用 `stock_fundamental_factors.report_period` + `factor_date` 严格早于 factor_date 至少 30 天

**因子 3: LowVol (低波动, 权重 0.30)**

- 目的: 挑波动小的 ETF (股价平稳)
- 原始值 (ETF 层直接算, **不下沉到成分股**):
  ```
  etf_lowvol_raw = z(-vol_60d) × 0.6 + z(-vol_20d) × 0.4
  ```
  - `vol_60d` = 过去 60 个交易日**每日 log-return** 的标准差 × sqrt(252) → 年化波动率. 取负 = 越低越好
  - `vol_20d` = 过去 20 个交易日的年化波动率
  - 60 天权重 0.6, 20 天权重 0.4 → 主要看中长期波动, 兼顾近月
- 数据源:
  ```sql
  SELECT time, close, prev_close
  FROM daily_bars
  WHERE stock_id = <etf_stock_id>
    AND time BETWEEN (factor_date - 90 天) AND factor_date
    AND is_trading_day = true
  ORDER BY time
  -- 计算 log_return = LN(close / prev_close)
  ```
- 缺失处理: 交易日数据缺失 > 5 天 → 该 ETF 当月剔除
- z-score in universe = 在全部候选 ETF 之间做横截面标准化

**因子 4: Momentum (动量, 权重 0.0 shadow only)**

- 目的: 观察题材/热点 ETF 是否有短期动量
- 原始值 (ETF 层):
  ```
  etf_momentum_raw = z(return_20d) - z(return_5d) × 0.3
  ```
  - `return_20d` = ETF 过去 20 个交易日累计收益 = close(t) / close(t-20) - 1
  - `return_5d` = 过去 5 日累计收益, **减去它** = 反转过滤 (短期猛涨的可能是过热, 打折扣, 参考 Hsu 2017 A股短期动量反转)
- 数据源:
  ```sql
  SELECT time, close FROM daily_bars
  WHERE stock_id = <etf_stock_id>
    AND time BETWEEN (factor_date - 30 天) AND factor_date
  ORDER BY time
  ```
- 用途: **不进入 total_score**, 但每月单独存 `momentum_shadow` 列, walk-forward 观察 6 个月后再决定是否入正式权重

**综合分公式**:

```
etf_total_score(t) =
    0.40 × z(etf_value_raw)
  + 0.30 × z(etf_quality_raw)
  + 0.30 × z(etf_lowvol_raw)
  + 0.00 × z(etf_momentum_raw)   -- shadow only
```

**Universe 定义** (排名池):
- 46-63 只 ETF 候选池 (§4.1 表格)
- 通过 L1 eligibility_gate (§5.2): 上市 ≥ 180 天, 日均成交额 ≥ 2000 万, 非停牌, 数据完整率 ≥ 90%

**排名 → BUY/SELL**:
```
top_5 = ETF_universe ORDER BY etf_total_score DESC LIMIT 5
top_6 = ETF_universe ORDER BY etf_total_score DESC LIMIT 6

current_holdings SET DIFF (top_6)  → SELL  (掉出 top 6)
top_4  SET DIFF (current_holdings) → BUY   (新进入 top 4, 严格用 top 4 避免边界抖动)
其余                                → HOLD
```

**数据 backfill 时间线** (支撑首次计算):
- daily_bars for 46-63 只 ETF + 所有成分股: 2022-01-01 至今 (T1)
- index_components: 每月末快照 (T2)
- stock_fundamental_factors: 季度快照 (T2)
- financial_reports 5 年年报: 2019-01-01 至今 (T3)
- 详见 §13.4 数据回填计划

**再平衡** (什么时候换股):
- 频率: 月度 (每月最后交易日 22:00 计算, **次月第一交易日 9:40 后分批限价/VWAP 执行**)
- 持仓: top 4-6 只 ETF, 单只 ≤ 15% (硬顶), 核心总仓位 ≤ 70% (硬顶)
- **仓位分配 (总仓位硬顶 70% + 按分数比例缩放)**: 先按因子总分给选中 ETF 分配目标权重, 再整体缩放到核心总仓位 ≤ 70%, 最后单只封顶 15%. 公式:
  ```
  raw_w_i    = etf_total_score_i / Σ(选中 ETF 的 score)   # 按分数分配
  scaled_w_i = raw_w_i × 70%                              # 缩放到总仓位硬顶
  final_w_i  = min(scaled_w_i, 15%)                        # 单只封顶
  # 若有封顶溢出, 将溢出部分按分数再分配给未封顶的 ETF (一轮再归一)
  ```
  举例: 选中 5 只, 分数接近 → 每只约 14% (总 70%), 都未碰 15% 顶; 若某只分极高 scaled 到 18% → 封到 15%, 多出 3% 分给其余 4 只。遵 §0.4 “规范 > 快”, 不靠“持仓数碰巧”兑现 70% 上限
- 换仓逻辑:
  - 排名滑出 top 6 → SELL (掉出前 6 卖掉)
  - 排名进入 top 4-6 → BUY (新进入前 4-6 买入)
  - 排名保持 → 持有不动 (还在 top 就别折腾)

**执行细节** (集合竞价 9:25 太粗, 会吃开盘价差、折溢价异常、流动性噪音):

原方案 "9:25 集合竞价" 会吃开盘价差、折溢价异常、流动性噪音, 侵蚀本就不厚的因子超额. 改成:

| 阶段 | 时点 | 方式 |
|---|---|---|
| Pre-check | 9:25 - 9:35 | 观察集合竞价出价、盘口深度、ETF vs 净值折溢价. 若折溢价 > 0.3% (溢价过高) → 推迟到 9:45 复评 |
| Split execution | 9:40 - 11:00 | 分 3-4 笔限价单 (限价 = 上一分钟均价 ± 0.05%), 每笔间隔 10-20 分钟 |
| VWAP fallback | 11:00 - 14:00 | 若未成交完, 剩余部分用 VWAP 算法或以当天 VWAP 为参考的限价 |
| Cutoff | 14:30 | 未完成部分**次日续做**, 不追高 |

**执行 gate** (以下任一触发, 该 ETF 当日 skip):
- 折溢价绝对值 > 0.5%
- 盘口买一/卖一距离 > 0.3%
- 当日成交额 < 前 20 日均值 × 30%

**回测要求**: 回测必须模拟 open / next-open / 9:40 / VWAP **四种执行价**, 输出**执行敏感性表** (哪种执行方式导致 alpha 差异多大).

**SELL 机制**: 只有月度再平衡触发, **不设单笔止损止盈**。ETF 波动小, 月度调仓足够风控。组合级 PR-L (整体大跌熔断) 保留。

### 4.2 卫星 20% — 题材事件驱动

**大白话**: 辅战场用**题材事件驱动**——系统监控市场热点 (AI / 光伏这种题材), 探测到某题材"启动"或"爆发"就买入相关个股, "退潮"就卖。风险大但可能有高收益, 所以**严格控上限**——单只股最多 5%, 整个卫星最多 20%, 亏太多直接冻结。

**依托**: `ThemeFermentationDetector` (题材发酵探测器 PR-O5) — 但**必须先做数据链修复** (见 §6.2)。

**触发** (BUY, 什么时候买):
- Detector 报"启动"或"爆发"阶段 (系统探测到某题材正在酝酿 / 爆发)
- 该题材未在冷却期 (不是刚亏过又来送死)
- 卫星总仓位未达 20% 上限

**触发** (SELL, 什么时候卖):
- Detector 报"高潮"或"退潮" (题材已到顶部或开始冷)
- **主动止损 -7%** (带缓冲, 可信号确认): 跌到自持仓成本 -7% 触发, 但过 EV/detector 判断——若同日 detector 仍报"启动/爆发" 且当日已企稳 (日内从低点反弹 >3%), 允许 T+1 再确认; 否则次日集合竞价平仓. 作用: 避免被正常回调洗出
- **硬止损 -15%** (无条件, 不看任何信号): 见下方硬边界, 是不讲理的最后防线
- 单笔 +20% 止盈 (赚 20% 落袋)
- 持仓满 21 个交易日 (硬时间, 到期无论盈亏都卖, 因为题材一般 3-4 周就冷了)

> **两道止损分工** (避免混淆): -7% 是 **主动止损**, 走信号 + EV 判断, 允许一天缓冲 (题材股日内波幅大, 硬性 -7% 即卖会被正常波动洗掉); -15% 是 **硬止损/深止损**, 不讲理、不缓冲、立即强平, 是兜底。正常情况下 -7% 先触发; 若 -7% 因缓冲未执行且行情继续恶化到 -15%, 则 -15% 硬止损兑现。

**仓位**:
- 单只题材股上限 5% (严于 ETF 的 15%, 因为个股崩得快)
- 目标持仓 3-4 只题材股
- 卫星总仓位上限 20%

**卫星硬边界** (防止无限吞钱):

```
□ 卫星总仓位 ≤ 20% (不允许扩)
□ 卫星【60 天滚动窗口】累计亏损 > 组合 5% → 卫星冻结 30 天, 只保留核心
    (30 万里, 过去 60 天卫星总亏 > 1.5 万 就冻结)
    起算方式: 每日盘后计算 (今日 - 60 天, 今日] 区间内所有 SELL 已实现 PnL
              + 期末未平仓的浮亏, 加起来 < -5% × 组合总资产 → 触发
    冻结解除后: 起算点不重置, 依然滚动 60 天窗口
□ 【自然月】连续 3 个月卫星月度 alpha < 0 → 卫星永久停止, 资金转入核心
    (alpha = 卫星月度 PnL% - 沪深 300 月度涨跌%)
    严格连续: 中间任何 1 个月 alpha ≥ 0 都重置计数
    冻结月计入: 冻结期间强制持仓不动, 仍算月度 alpha (通常为 0 或轻微负)
□ 单只题材股【自持仓成本】-15% (硬止损/深止损, 无条件强平, 不看信号) → 强制平仓。与上方主动止损 -7% 分工: -7% 可缓冲, -15% 不可缓冲
```

**卫星是探索沙盒, 核心永远是主战场**。别把辅战场当主力用。

### 4.3 现金 10%

**大白话**: 剩下 10% 别闲着, 分两份——一半留应急 (家里急用), 一半买最保守的国债 ETF 或货币基金吃个 2-3% 的年利息, 比银行活期多。

- 5% 应急现金 (余额宝, 年化 2%)
- 5% 收益现金 (国债 ETF 511010 或短融 ETF 511360, 年化 3%)
- 不做股票短线操作 (这 10% 是压舱石, 别拿去赌)

### 4.4 卫星题材股 T+1 Entry 规则

**大白话**: A 股规则今天买明天才能卖, 且个股有涨停 (最多涨 10%)。**这条规则只针对卫星题材个股** (§4.2)—— 核心 ETF (§4.1) 不受此规则约束, ETF 无涨停打不进问题, 直接集合竞价。如果系统今天说该买某题材股, 但收盘涨停了, 明天开盘可能一开就涨 5-10%, 追进去容易被套。这里定义了几种情况下怎么应对。

**适用范围**: 只针对 §4.2 卫星题材股. 核心 ETF 直接集合竞价 entry, 忽略此规则.

针对卫星题材股的涨停 / 高开问题:

```
if 信号 T 收盘涨停 (今天到顶了):
  → 允许"限价追高 3%" (T+1 集合竞价挂"高开 3% 以内成交"的限价单)
     打得进 → 成交, 进场
     打不进 (一字板 = 全天挂涨停, 抢不到) → T+2 观察, 继续涨停则放弃, 开板 (跌下涨停) 则追入

elif T+1 高开 > 5% (明天开盘一下涨 5% 以上):
  → 观察 15 分钟后决定 (可能是炒作短线冲高, 15 分钟内往往回落, 等等看)

elif T+1 高开 < 3% (明天开盘涨得不多):
  → 集合竞价 entry (开盘直接买)
```

(核心 ETF 不受此规则约束, 已在标题上方说明.)

---

## 5. 自动化 + confidence

### 5.1 confidence 模型 (Wilson lower bound + 完整校准)

**大白话**: 每条信号旁边要显示"置信度 X%", 但**不能只用简单胜率**——样本少时 40%/55% 可能只是噪音, 且忽略盈亏比、回撤路径、成本, 用点估计直接 sizing 会**系统性过度自信**. 因此:
1. **confidence 用 Wilson 下界** (置信度 90%), 不用裸胜率
2. 同时存 5 个校准指标: `n_samples / avg_win / avg_loss / profit_factor / brier_score`
3. UI 显示时给出 reliability bucket ("样本少置信度差", "样本足胜率稳"), 让用户看到样本量, 而不是被光鲜的百分比误导

**核心公式**:
```
n = detector 过去 90 天信号数
k = 其中赚钱的信号数
p_hat = k / n  # 裸胜率
confidence = Wilson_lower_bound(k, n, α=0.10)  # 90% 置信下界
```

Wilson 下界公式:
```
z = 1.645  (α=0.10, 单侧 90%)
denom = 1 + z²/n
center = (p_hat + z²/(2n)) / denom
margin = z × sqrt(p_hat×(1-p_hat)/n + z²/(4n²)) / denom
confidence = center - margin
```

**举例**:
- Detector A: n=100, k=55 → p_hat=55%, confidence (Wilson lower) = **47.5%**
- Detector B: n=10,  k=6  → p_hat=60%, confidence (Wilson lower) = **34.6%**

裸胜率 B 更高, 但 Wilson 下界表明 B 样本太少不可信, A 更靠谱. 系统按 confidence (Wilson) 排序.

**同时存的 5 个校准指标**:

| 指标 | 定义 | 用途 |
|---|---|---|
| `n_samples` | 90 天内该 detector 结算样本数 | 小于阈值 (< 20) 时降权或只跑 paper |
| `avg_win_pct` | 赚钱信号的平均收益 % | EV 公式用 (§5.2) |
| `avg_loss_pct` | 亏钱信号的平均亏损 % (绝对值) | EV 公式用 |
| `profit_factor` | Σ 盈利 / Σ 亏损 | > 1.5 才算稳定盈利策略 |
| `brier_score` | 概率校准分数 (0=完美, 0.25=瞎猜) | 每周输出 reliability diagram, 越低越好 |

**Reliability bucket** (UI 展示):

| Wilson 下界 | n_samples | 展示标签 | 是否允许下单 |
|---|---|---|---|
| ≥ 45% | ≥ 30 | 🟢 "样本充足, 胜率稳定" | 是, 全权重 |
| 30-45% | ≥ 30 | 🟡 "样本充足, 胜率中等" | 是, 但 sizing × 0.7 |
| ≥ 45% | 20-29 | 🟡 "样本偏少, 建议观察" | 是, sizing × 0.5 |
| 30-45% | 20-29 | 🔴 “样本偏少且胜率中等” | **纸面模式**, 不进 EV gate |
| < 30% 或 n < 20 | - | 🔴 "样本不足或胜率差" | **纸面模式**, 不进 EV gate |

**数据源**: `recommendation_trade_outcomes` (数据库表, 存了每条信号的实际结果) 按 source_type (哪个模块给的) group by

**窗口**: (signal_date - 5 天 - 90 天, signal_date - 5 天), 5 天滞后因 forward_return (未来 5 天收益) 需 5 天才能结算

**缓存**: 每 detector 每天算一次, 5 指标全刷新

**三条管线** (A-market / B-fusion / C-bullish) 全部改用此 service, 不再用 score 当 alias

**服务**: 新建 `ConfidenceCalibrationService` (专门算 Wilson + 5 校准指标的模块)

**冷启动 (新 detector 没历史数据咋办)**:
- n < 20 时**默认纸面模式** (不进 EV gate, 不下实盘), 累积样本
- 参考同类 detector prior 时明确标注 "prior from `xxx-detector`", 不能骗自己

**Regime 分层**: 不同市场状态 (牛/熊/震荡) 下同一 detector 表现差异大, 因此:
- confidence 按 regime 分别计算 (`confidence_bull` / `confidence_bear` / `confidence_range`)
- 当前 regime 判定用沪深 300 60 日均线斜率
- 使用时取当前 regime 对应的 confidence, 而不是全局混合

**必然滞后 5 天**: UI 显式标注 "Confidence 38% (Wilson lower, n=45, 基于 04-01 至 06-25), 最近 5 天尚未结算"

**参考来源**:
- scikit-learn Probability Calibration (Brier score, reliability diagram)
- Bailey et al., Probability of Backtest Overfitting

### 5.2 Gate 分层

**大白话** (**修订**): "ETF 核心不用 EV gate" **不等于** "ETF 核心不用 gate". 月度因子排名只解决 "买哪个" 的问题, **不覆盖数据缺失/流动性/折溢价/组合风险/执行成本/PR-L 熔断** 这些执行风险. 因此把 gate 分成 4 层, ETF 核心可跳过卫星专用的 **EV gate**, 但必须过其他 3 层.

**Gate 4 层结构**:

| 层 | 名称 | 检查内容 | ETF 核心 | 卫星题材 |
|---|---|---|---|---|
| L1 | `eligibility_gate` | 数据完整性 / 池成员 / 停牌 / 流动性 / 上市时间 | ✅ 必过 | ✅ 必过 |
| L2 | `risk_gate` | 单仓上限 / 单板块上限 / 组合回撤 / PR-L 熔断 | ✅ 必过 | ✅ 必过 |
| L3 | `cost_gate` | 换手率 / 冲击成本 / 折溢价 / 双边成本 | ✅ 必过 | ✅ 必过 |
| L4 | `ev_gate` | 期望值 > 门槛 (胜率 × 平均赚 - (1-胜率) × 平均亏) | ⛔ 跳过 (月度因子排名替代) | ✅ 必过 |

**具体阈值**:

**L1 eligibility_gate**:
- 上市 ≥ 180 天
- 日均成交额 ≥ 2000 万 (过去 20 日均)
- 当前非停牌
- 池成员 (核心 = ETF 池, 卫星 = 题材池)
- 数据完整性: 过去 90 日无 > 5 个交易日缺失

**L2 risk_gate**:
- 单仓上限 (核心 ETF 15%, 卫星题材 5%)
- **核心总仓位 ≤ 70% (硬顶)**: 单仓 15% × 持仓数可能超 70%, 故总仓位单独设硬顶, 超限时按 §4.1 比例缩放
- 单板块 ≤ 25% (继承 PR-M4)
- 组合最大回撤 (60 日) < 20%
- PR-L 未触发 (若触发则冻新单, §5.4)

**L3 cost_gate**:
- ETF 折溢价绝对值 ≤ 0.5%
- 盘口买一/卖一距离 ≤ 0.3%
- 本次调仓换手率 ≤ 15% (单次), 年化 ≤ 200% (12 个月滚动)
- 冲击成本估算 (基于 20 日日均成交额 vs 本次订单量): 单笔冲击 ≤ 0.2%

**L4 ev_gate** (只对卫星, ETF 跳过):
- 公式: `EV = confidence × avg_win_pct - (1 - confidence) × avg_loss_pct`
- 阈值: EV > 0.5% 才下单 (锚定手续费 0.13% × 2 + 滑点 0.2-0.5%)

**举例** (卫星 BUY 信号):
- 胜率 40%, 平均赚 15%, 平均亏 8% → EV = 0.4 × 15% - 0.6 × 8% = **1.2%** → 下单
- 胜率 60%, 平均赚 3%, 平均亏 5% → EV = 0.6 × 3% - 0.4 × 5% = **-0.2%** → 拒绝 (虽然胜率高但赚小亏大)

**Gate 组合决策**:

```
if not L1_pass: skip (不合格, 记 gate_reason='eligibility_fail')
elif not L2_pass: skip (风控, 记 gate_reason='risk_fail')
elif not L3_pass: skip 或推迟 (成本, 记 gate_reason='cost_fail', 尝试次日)
elif signal_type == 'satellite' and not L4_pass: skip (EV 不足)
else: gate_pass = true
```

**参考来源**:
- Transaction Costs of Factor-Investing Strategies (ResearchGate 331912322)
- Rebalancing With Transaction Costs (SSRN 3495461)

### 5.3 自动化和用户拍板同源

**大白话**: 不管是人自己看信号决定买不买, 还是以后系统自动下单, **看的是同一份信号 + 同一份 EV gate 结果**, 不搞两套。区别只是"人 vs 机自动扣扳机"。

- 用户拍板: 所有信号 + gate_reason (通过/拒绝的原因) 展示给用户, 用户自己决定
- 自动化: gate_pass = true 才下单
- 两条路消费同一信号 + 同一 gate, 只在"是否触发下单"分岔

**用户 override (违反系统建议直接下单)**:
- UI 二次弹窗
- 手动写理由 (≥ 20 字, 入库)
- 记录 override 编号

**Override 冷冻规则**:
- 触发: 累计 override ≥ 10 次且亏损占比 > 60% (老不听系统还亏得多, 说明你在瞎搞)
- 分 regime (市场状态): 熊市阈值 = 80% (熊市谁都亏, 稍微宽松)
- 触发后冷冻 30 天, 期间只能跟系统信号

### 5.4 PR-L 与优先级

**大白话**: 如果整个组合大跌触发**紧急停损 (PR-L)**, 系统进入"防御模式"——不再开新仓, 但已有仓位的单笔止盈止损照常执行 (还是要保护已有的钱)。多种保护指令冲突时按下面顺序判断。

```
if PR-L 紧急停损触发:
  新信号: 全部 skip
  已持仓: 单笔止盈止损仍生效 (仓位保护优先)
  已触发 SELL 后: 转观察, 不再自动开新仓

优先级: PR-L 冻新单 > 单笔保护 > 时间止损
```

**PR-L 过渡期解除条件** (系统亏了太多进入紧急保护后, 什么时候恢复正常):

> **Day0 起点**: 此处 Day0 指 **PR-L 触发当日**, 与 §9 重构时间线的 Day0 (重构启动日) 无关。重构启动时系统状态完全重置、PR-L 归为未触发态, 本过渡期表仅在未来某次 PR-L 熔断后才激活。

- Day 0-90: PR-L 保持 (实盘几乎冻结, 只跑纸面)
- Day 91-150: PR-L 部分解除 (允许小仓位, 5% 单仓)
- Day 151+: 完全解除条件 = "主线累计 ≥ 30 笔 且 win% ≥ 40%" (证明策略确实转好了)

---

## 6. 数据 + 修复

### 6.1 数据源 (精简版)

**大白话**: 系统要吃数据 (新闻 / 公告 / 行情) 才能生成信号。数据源分**主渠道** (合规稳定的, 支撑主线) 和**卫星辅助** (轻量级 LLM 用于题材分类)。**不用**爬虫和付费终端 (合规风险 + 超预算)。

**主渠道 (合规, 主线数据基础)**:
- 新浪财经 / 财联社 RSS (合规订阅源)
- 证监会 + 交易所公告 API (官方数据)

**卫星辅助 (轻量)**:
- LLM 分类 (Claude Haiku 或 GPT-4o-mini) —— 让 AI 判断某新闻属于哪个题材
- 关键词字典兜底 (LLM 服务不可用 / API 故障时降级到硬编码关键词匹配)

**排除**:
- 雪球 / 东财爬虫 (合规风险 — 未授权爬人家网站)
- 微博 / 微信 / 抖音 (没免费办法拿数据)
- 同花顺 (反爬极严, 短时间被封)
- 付费 iFinD / Wind / Choice (万八千一年, 超预算)

**每周 15 分钟人工 review**: LLM 提议题材归一化 (例如"算力" → "AI"), 用户点确认 / 拒绝。此环节**必须保留人工确认, 不做全自动** (LLM 幻觉——瞎编题材——会造成累积混乱)。

**工作量**: 3-4 天 (原规划 8-10 天, 因主线降为混合结构缩减)

### 6.2 PR-O5 数据链修复 (卫星前置)

**大白话**: 卫星策略依赖题材探测器 (`ThemeFermentationDetector`), 但目前它的**上游数据**是空的 (没抓到数据), 所以探测器什么信号都没产出。上线卫星前必须先把这条数据链修好。

**问题**:
- Cron 2026-06-30 才 seed (定时任务最近才开始跑), 之前 90 天没跑过
- 上游 `LIMIT_UP_SYNC` (涨停股同步) 当天 fetched=0 (没抓到东西), 全链空跑
- 代码 explicit 声明不写 `ai_investment_signals` (soft-layer 设计, 探测器输出没进主信号表)

**修复方案**:

**A. 修上游 (必做, 2-4 h)**:
- 修 `LIMIT_UP_SYNC` 数据抓取 (让涨停股数据真的能进来)
- 修 `resolveTradeDate` 挑"最近有 limit_up_stocks 的交易日"而不是"今天" (今天可能是周末没数据)
- Backfill 90 天历史 (2026-06-02 至 2026-06-29)

**B. 让 detector 写 signals 表 (1-2 天)**:
- 加 step 7: fan-out top_codes → `AIInvestmentSignal` (让探测器输出真正进主信号表)
- source_type = `'theme_event'` (独立, 不再借 `quant_recommendation` 表位)
- source_id = `theme:<industry>:<trade_date>:<symbol>` (每条信号可追踪)
- decision 映射: launch / outbreak → BUY, climax → HOLD / SELL, recession / germinate → skip
    (启动/爆发 → 买, 高潮 → 持有或卖, 退潮/发芽 → 跳过)
- metadata: phase, is_mainline, industry, timing_tag='theme_phase'

**严格顺序**: A → 观察 1 交易日 → B → 观察 3-5 天 → 才能接入 §5 confidence + EV gate。急不得。

---

## 7. 删码清单

**大白话**: 这一章列出**哪些代码留着, 哪些改, 哪些删**。原则见 §0.1 那个二维矩阵。

### 7.1 保留

| 模块 | 说明 |
|---|---|
| `ThemeFermentationDetector` (PR-O5) | 卫星核心 (题材发酵探测器), 需先修复 §6.2 |
| `AnalysisEngineService` / tradingagents | 辅助研究模块, 月度调仓时给用户看行业分析 |
| `BullishEventDetectorService` | 卫星事件源 (改 source_type 独立不再借 quant_recommendation) |
| `AIInvestmentSignal` 主表 | 扩 schema (§2.2 那个 Signal 结构) |
| `recommendation_trade_outcomes` | 加 rebalance_id, theme_id 字段 (记录每单是核心再平衡还是卫星题材) |
| `BlackSwanDetectorService` | 风控类, 探测市场极端事件 (§0.2 例外) |
| PR-L 紧急停损 | (§0.2 例外, 保命的) |
| PR-M4 仓位风控 | 阈值上调到单仓 15%, 单板块 25% |
| PR-M1/M2/M3 数据层 | ETF 因子需要这些数据基础设施 |
| PR-N 数据修复 | 保留 |
| `V3RecommendationController` | 重构展示逻辑 (ETF 排名 + 卫星题材) |
| `paper_trading_automation` 框架 | 重构下单逻辑接入 EV gate + 月度再平衡 |
| Phase 11-16 UI 基础布局 | 装饰组件删 (那些扫光/飞线/3D 特效砍掉) |
| 战法库文档 (PR-I-v2) | 历史 reference |

### 7.2 改思路或重写

| 模块 | 处置 |
|---|---|
| 三条管线 confidence | 全部改用 `ConfidenceCalibrationService` (§5.1, 用真实胜率而非规则打分) |
| `paper_trading_automation` 下单排序 | 改用 EV gate 而非 `confidence_score DESC` (按期望值排, 不是按虚假置信度) |
| Phase 11-16 视觉装饰 | 扫光 / 飞线 / 3D tilt 删, 保留基础卡片 (少一些花哨效果, 更专注内容) |

### 7.3 删除

**思路错 + 在用** (这些是有害的):
- `QuantFusionService` 整套 (95 笔 0% 毒源, 伪共识 + 规则封顶 —— 就是那个 20 策略同质化的祸首)
- 20 个 per-stock 策略 (同质化趋势 —— 20 个都是"追涨"变种, 全是一种思路)

**思路对但不在新方向** (思路本身没错, 但和新方向不搭):
- `archiveQuantRecommendations` (A 路径, 全市场选股)
- `QuantRecommendationService`
- `OpeningRushDetector` (PR-O3, 日内, 我们不做日内)
- `IntradayPriceVolumeAnomalyDetector` (日内)
- `LastHourMomentumDetector` (日内)
- `AfternoonKickDetector` (PR-O6, 日内)
- `LimitUpBoardDetector` (PR-O2, 短线, 卫星不需要)
- `IntradayMomentumDetector` (日内)
- `IntradayReversalDetector` (日内 0 产出)
- `MarketTopDetector` (辅助诊断)
- `BehaviorBiasDetector` (辅助诊断)
- `QALeadingSignalDetector`
- `IntradayOpportunityWatcher` (盘中 10 子规则)
- `syncDailyScreener` (全市场扫盘)
- 通知 PR-D/E 个股利好

### 7.4 新建

| 模块 | 工作量 |
|---|---|
| `ETFRotationService` (含因子计算 + 月度再平衡) | 3-4 天 |
| `FactorCalculatorService` (Value/Quality/LowVol/Momentum 4 个因子) | 2 天 |
| 因子历史回测脚本 (2022-2026, Week 2 验证) | 1-2 天 |
| 闲置现金管理 (国债 ETF / 货币基金自动配置) | 1 天 |
| `ConfidenceCalibrationService` (胜率计算) | 1 天 |
| `AutoExitService` (EV gate 自动止盈止损) | 1-2 天 |
| 精简数据源 (RSS + 公告) | 3-4 天 |
| PR-O5 修复 (§6.2 A + B) | 2-3 天 |
| 战略镜子 UI + monthly_metrics.sql | 1 天 |
| **合计** | **15-19 天** |

**估算**:
- 删除: 后端业务代码 40-50%
- 改写: 3 处
- 新建: 15-19 天

---

## 8. 战略镜子

### 8.1 设计原则

**大白话**: 每月自己坐下来花 30-60 分钟, 用 6 道题看清"这个月赚亏的原因是什么, 下月怎么办"。每题必须写 4 层, 不能跳。**AI 只填客观数据, 主观归因和决定必须人写**——不然就变成"AI 说的算" 你就退化了。

每题 4 层固定 (不能跳):

```
层 1: 现状  → 数据是什么 (AI 填, 客观)
层 2: 评估  → 数据相对目标偏多远 (AI 算)
层 3: 归因  → 偏在哪里 (AI 建议, 用户手写至少一条)
层 4: 决定  → continue / 小调 / 切换 / 全停 (用户签字)
```

**分工**: AI 全填数据 + 建议, 用户 override。**必须至少一条归因手写**, 否则 §8.3 判定退化 (机制无效)。

**存放**:
- 模板: `docs/PROJECT_COMPASS.md`
- 月度归档: `docs/compass/YYYY-MM.md` (例如 `2026-07.md`)
- 数据 SQL: `scripts/compass/monthly_metrics.sql`

**节奏**: 每月第一个周末填一次。

### 8.2 6 题

| # | 主题 | 决定输出 |
|---|---|---|
| Q1 | 核心 (ETF 因子) 实盘表现 | continue / 调因子权重 / 切换 / 全停 |
| Q2 | 核心因子稳定性 | 因子不动 / 调权重 / 删加因子 |
| Q3 | 卫星 (题材) 表现 + 硬边界 | 卫星继续 / 冻结 / 永久停 |
| Q4 | 核心 vs 卫星 Sharpe 对比 | 比例不动 / 缩卫星 / 停卫星 |
| Q5 | 镜子机制自检 | 维持 / 调题目 / 暂停镜子 |
| Q6 | 下月承诺 (ToDo + 边界 + 数据焦点) | 具体到数字 |

### 8.3 硬阈值 (切换 / 卫星停 / 主线证伪)

**卫星停止** (触发任一条件都停):
- 60 天滚动窗口累计亏损 > 组合 5% → 冻结 30 天 (详见 §4.2 卫星硬边界)
- 自然月连续 3 个月月度 alpha < 0 → 永久停止 (中间任 1 个月 alpha ≥ 0 都重置)
- 单只 -15% (自持仓成本口径) → 强制平仓

**主线切换** (影响不到, 因为混合结构没有独立影子)

**主线证伪** (证明主线不行):
- 累计 ≥ 30 笔样本 (Wilson CI 有意义, 样本太少的结论不可信)
- 分 regime (牛熊震荡分开) 统计
- 连续 2 个月 win% < 30% 且 regime 相同 → 触发 §10 降级考虑

**镜子退化** (机制本身失效):
- 连续 2 个月手写归因 < 1 条 或 用户填写 < 5 分钟 → 暂停镜子机制 (你在应付了)

### 8.4 Q6 承诺硬约束

- ToDo 最多 5 项 (超过 5 项证明贪多, 一个都做不好)
- "不做"最多 3 项 (明确划边界)
- 数据焦点必须具体到数字 (例如"实盘 win% 从 0% 提升到 30%", 而不是"表现变好"这种模糊话)

---

## 9. 过渡期实施路径

**大白话**: 从今天到卫星上实盘, 大概 90 天。前 3 天冷静, 然后 3 波开发 (14 天基础 → 7 天 ETF 回测 → 14 天上核心), 期间实盘几乎冻结 (先跑纸面), 卫星 60 天后如果表现 OK 才上实盘。

### 9.1 时间线

| 阶段 | 天数 | 内容 |
|---|---|---|
| **冷却** | Day 0-3 | 重读全文, 校准 §11 待校准数字, 决定是否仍认同 §3 锁定 |
| **Wave 1** | Day 4-14 | 建 confidence / EV gate / ETF 框架 / 数据源 / PR-O5 修复 |
| **Wave 2** | Day 15-21 | ETF 因子回测 (2022-2026), 过 §9.2 七关验收才主线上线 |
| **Wave 3** | Day 22-35 | 删码, 核心 ETF 首月小仓灰度上线 (30 万 × 30% × 60% = 5.4 万, 见 §9.2) |
| **观察** | Day 36-60 | 核心稳定, 卫星纸面积累 30-45 天数据 |
| **扩展** | Day 60+ | 卫星表现 OK 转实盘 (20% 上限) |
| **全量** | Day 90+ | 首次填战略镜子 |

### 9.2 关键分岔点

**Wave 1 → Wave 2** (基础建完能进入回测阶段吗):
- B-fusion 已停 (auto_trade off, 毒源已经不再运行)
- 实盘完全冻结
- 所有新 service 单测通过

**Wave 2 因子回测**: **上线验收标准**

**大白话**: 原来"2022-2026 回测年化达标即上线"判据偏快. 单一样本窗口 + 多重参数选择 (因子权重 / ETF 池 / 调仓规则 / 成本) 很容易选出**历史最好看但未来失效**的组合. 因此上线前必须通过下面**7 道验收关**, 全部达标才允许 Wave 3 小仓灰度.

**验收 7 关** (P0 硬约束):

| # | 关卡 | 定义 | 阈值 (V0 candidate) |
|---|---|---|---|
| 1 | **成本后年化** | 扣除双边手续费 0.13%×2 + 滑点 0.2-0.5% 后的年化收益 | ≥ 10% |
| 2 | **成本翻倍压力测试** | 手续费 × 2、滑点 × 2 (模拟实盘更差成本), alpha 仍要 > 0 | alpha > 0 |
| 3 | **Walk-forward** | 2022-2024 训练, 2025 测试; 2022-2025 训练, 2026 测试; 每年滚动一次 | 每期测试年化 ≥ 8% |
| 4 | **CSCV / PBO** | Combinatorially Symmetric Cross-Validation, 计算 Probability of Backtest Overfitting | PBO < 0.5 (Bailey et al.) |
| 5 | **参数扰动** | 因子权重网格 {(0.4/0.3/0.3), (0.35/0.35/0.3), (0.5/0.25/0.25)}, 三个 ETF 池 (V0 46 只 / 减 5% / 加 5%), Momentum shadow on/off | 各组合年化差异 ≤ 2%; 最差组仍 ≥ 8% |
| 6 | **最近 12 个月 OOS** | 2025-07 至 2026-06 (完全未训练), 单独跑一次 | 年化 ≥ 8%, 最大回撤 ≤ 25% |
| 7 | **Regime 分层表现** | 分牛 / 熊 / 震荡 (用沪深 300 60 日均线斜率界定), 每 regime 至少 6 个月样本 | 每 regime 年化 ≥ 5% (熊市可为负但 > -10%) |

**判定**:
- 7 关全过 → 主线上线, 目标 10-15% (原 12% 挡)
- 6 关过, 1 关介于阈值 ±10% → 部分采纳: 目标下调到 8-10%, 上线**灰度 30%** (仓位打 3 折)
- 5 关或以下过 → 主线不上线, 重新评估因子权重, 或触发 §10.4 全面降级

**参考来源**:
- Bailey et al., The Probability of Backtest Overfitting
- Invesco, Factor Investing in China: Beware Structural Breaks

**Wave 3 上线条件** (可以真上实盘吗):
- Wave 2 上线验收 7 关全过或 6 过
- 新 service 观察 3 天无 bug
- **首月强制小仓灰度**: 核心仓位 30 万 × 30% × 60% = 5.4 万 (原方案 9 万的 60%)
- 灰度期间每周做 live-vs-backtest drift report

**卫星上实盘 (Day 60+)**:
- 卫星纸面 30 天 win% ≥ 45% (纸面模拟能赢一半, 才有资格上真钱)
- 未触发 §4.2 硬边界
- Brier score < 0.20 (confidence 校准良好)

---

## 10. 失败降级路径

**大白话**: 主线依赖 3 个前置 (数据源 / 卫星探测器修复 / confidence 冷启动), 任一失败, 都有对应的**降级方案**——不硬撑, 直接砍功能保命。三个都失败, 只保留核心 ETF, 卫星彻底放弃。

主线依赖 3 个前置, 任一失败, 降级如下:

### 10.1 §6.1 数据源失败

**触发**: 8 天内 RSS 源失效 / 公告 API 不可用 / LLM 服务持续不可用 (API 故障或降级无解)

**降级**:
- 砍备用渠道, 只用主渠道 (新浪 RSS + 证监会 API)
- LLM 分类关闭, 只用规则关键词字典 (硬编码"AI关键词"当分类)
- 卫星 alpha 减半, 硬边界从 5% 缩到 3% (数据质量降了, 更严格控风险)

### 10.2 §6.2 PR-O5 修复失败

**触发**: 上游 `LIMIT_UP_SYNC` 超 3 天调试无果 (数据抓不进来)

**降级**:
- 卫星推迟到 Q4 (等数据源恢复)
- 卫星资金转入核心 (核心 90% + 现金 10%)
- 目标降到 8-12% (纯 ETF 因子上限, 没了卫星就少了博高收益的部分)

### 10.3 §5.1 confidence 冷启动失败

**触发**: default 30% 先验下, 卫星纸面阶段连续 5 笔亏损 (2 周内) —— 说明用 30% 作先验太乐观, 冷启动纸面就不及格 (n<20 本就不下实盘, 见 §5.1)

**降级**:
- 卫星维持纸面, 冻结 30 天不得毕业上实盘
- confidence default 从 30% 下调到 20% (更悲观, 更多信号被 EV gate 挡掉)
- 30 天后基于纸面数据重新评估

### 10.4 全部前置失败的终极降级

**触发**: 三个前置在 60 天内都跑不通

**处置**:
- 核心继续 (ETF 因子学术支持稳, 至少这块能吃)
- 卫星彻底放弃
- 组合结构变为 核心 80% + 现金 20%
- 目标降到 8-12%
- 战略镜子开特殊 review, 讨论项目是否继续 (值不值得继续投入时间)

---

## 11. 数字锚定参考

**大白话**: 所有关键数字都必须有出处 (学术论文 / 历史数据 / 经验值)。**禁止拍脑袋加新的 magic number**。已锚定的 vs 待校准的分开列。

### 11.1 已锚定 (有依据, 可直接使用)

| 数字 | 依据 |
|---|---|
| 组合年化 底线 8-10% / 冲刺 10-15% | 学术锚 (Fang 2020, de Groot 2021 A 股因子长期收益论文); 底线仅靠核心, 冲刺需卫星打满, 详见目标层 / §3 |
| ETF 单仓 15% | 单股黑天鹅 -50% 时组合 -7.5%, 一次可承受 |
| 题材单仓 5% | 卫星波动大, 严于核心 |
| 单板块 25% | 继承 PR-M4 已有约束 |
| EV gate 0.5% | 手续费 0.13% × 2 + 滑点 0.2-0.5% |
| 因子权重 V0.40 Q0.30 L0.30 M0.0 (shadow) | 学术支撑 (JFE 2019 Value / JAM 2021 LowVol / Hsu 2017 Momentum 降级), §4.1 V0 candidate, 待 §9.2 七关验收 |
| Wilson CI 样本 N ≥ 30 | 统计显著性下限 (小于 30 笔的结论不能信) |
| override 冷冻 10 次 / 60% | 平衡"允许探索"和"防止失控" |
| 数据源工作量 3-4 天 | 主线降为混合后精简 |
| 主线上线验收成本后年化 10% | 底线目标下限, §9.2 七关之第 1 关; 适配双层目标 |

### 11.2 待校准 (回测数据决定, 冷却期或 Wave 1 校准)

| 数字 | 待校准依据 |
|---|---|
| 卫星累计亏损 5% 组合冻结 | 需回测确认 A 股题材月度亏损分布 |
| 卫星单笔止损 -15% | 需回测题材股月度回撤分布 |
| 卫星止盈 +20% | 需回测题材股月度上涨分布 |
| 卫星持仓 21 天 | 需回测 A 股题材平均生命周期 |
| 连续 3 月负 α 停 | 拍脑袋, 待样本积累后调整 |
| 主线切换 Sharpe 差 0.3 | 统计经验值, 需回测确认 |
| 主题事件驱动私募基金公开净值 | 私募排排网 / 华宝证券私募库, Deep Research 未通过对抗验证, 待补证 |
| risk-managed momentum A 股复现 | Barroso 2015 是美股, 需 Wave 2 A 股独立回测 |
| A 股主题 ETF 5 年跑输具体数据 | 需选 1 只主题 ETF + 沪深 300 5 年区间回测 (原引 Ben-David 是美股) |
| 上交所 2020-2024 散户换手率 | Fact Book 官方索引 2020-2024 段缺失, 需从深交所年报 / 证监会 / 中证登补 |

### 11.3 数字锚定原则

**冷却期任务**: 遍历 11.2 每个数字, 若能找到学术 / 历史数据支持则改到 11.1; 若无法, 保留 "待动手时校准" 标签。

**动手时禁止拍脑袋加新 magic number**: 新的数字必须有 11.1 或 11.2 之一的锚定 (来源要能说出来)。

---

## 12. 元教训 (工作方法层)

**大白话**: 这些是**做这个项目的过程本身**教会的经验, 不是股票策略, 但影响后续所有决策。

这些是这次会话本身的产出, 不是项目内容, 但影响后续所有工作:

### 12.1 讨论方法

- 单次会话极限 = 走出方向, 不是产出可执行方案 (一次讨论最多定"往哪走", 别指望一次讨论完就能开工)
- 每次锁定大决策前, 用 sub-agent 模拟"苛刻外部审计员"挑刺 (让 AI 反串审计员来找漏洞)
- 审计员发现的漏洞会推翻锁定 (今天发生过一次: 题材 → ETF 混合)
- AI 会默认接住用户方向 (确认偏差), 除非被明确要求挑战 —— 你说啥 AI 都说"你说得对", 得主动要求它反驳

### 12.2 数字使用

- "持仓部分年化" vs "组合年化" 是单位差异, 混用会造成 60-70% 期望落差
    (卫星本身年化 50% 听着牛, 但只占 20% 仓位, 对组合贡献只有 10%)
- "梦想数字" (例如 50% 年化) 若不校准, 会绑架整个方案设计
- 沉没成本偏差最明显的迹象: "看机会加仓" / "先留着以防万一" —— 出现这种话就是在为已经亏的东西找借口

---

## 13. 运维保障 (回滚 / 监控 / 数据回填)

**大白话**: 每次改代码都可能出问题。这一章讲**出问题咋办**——怎么快速回滚、怎么监控预警、怎么补历史数据。

### 13.1 回滚方案

**核心原则**: 每次删码 / 改写 / 新建都必须**可回滚**, 且回滚时间 < 15 分钟。

**分级回滚**:

| 级别 | 触发场景 | 回滚方式 | 耗时 |
|---|---|---|---|
| L1 单 PR 回滚 | 某个新 service 上线后 bug | `git revert <commit>` + 重新部署 | 5-10 分钟 |
| L2 主线整体回滚 | 核心 ETF 主线首月负 alpha 严重 | 切回 auto_trade off + PR-L 冻结 | 10 分钟 (无需回滚代码, 只关开关) |
| L3 版本回滚 | 大范围失败, 需要回到之前版本 | 从 `releases/` 目录切 symlink | 15 分钟 |
| L4 数据回滚 | DB schema 变更后失败 | 从每日备份恢复 (pg_dump 已存在) | 30-60 分钟 |

**关键约束**:
- **B-fusion 代码删除前必须先跑 30 天新自动化层影子对比**, 确认新逻辑不比 B-fusion 差之后再删 (不能一下子拆房子)
- **删码前必须打 tag**: `git tag pre-delete-b-fusion-2026-07-XX`, 便于紧急恢复
- **DB 变更必须两阶段**: 先加字段 (向后兼容), 观察 7 天, 再删字段
- **禁止**: 单个 PR 里同时删多个模块的代码 (哪个坏了不知道)

**回滚决策权**:
- L1/L2: 用户单独决定
- L3: 用户 + 战略镜子 Q5 讨论
- L4: 必须留证据 (log + 数据快照), 战略镜子专项 review

### 13.2 监控告警

**监控分 3 层** (从底层到业务):

**系统层** (基础设施):
- Cron 任务 stuck / 失败 → 飞书 OPS 告警
- DB 连接失败 / 慢查询 → 飞书 OPS
- 磁盘 / 内存超阈值 → 飞书 OPS
- (保留 PR-A / 已有基础设施)

**业务层** (信号 / 交易):
- 主线月度再平衡未触发 (次月第一交易日 10:00 仍无信号) → 用户飞书通知
- 卫星连续 3 笔亏损 → 用户飞书通知 (提前警示硬边界)
- Confidence 计算失败 (依赖数据缺) → 用户飞书 + 战略镜子 Q1 数据标"未知"
- Override 触发 → 用户飞书通知 + 入库审计
- PR-L 触发 → 用户飞书**紧急**通知 (置顶)

**指标层** (性能 / 趋势):
- 每周日晚生成一份指标简报 → 飞书自己
  - 本周组合收益率
  - 核心 vs 卫星 PnL 分解 (盈亏分别多少)
  - 卫星硬边界距离 (剩余 X% 触发)
  - Override 次数 / 亏损占比
- 战略镜子月度自动 draft → 飞书自己

**告警渠道**:
- 用户飞书 OPS 群 (已有基础设施, PR-C 通知中心)
- 不发短信 / 电话 (成本 + 打扰)
- **不发用户手机 push** (手机推送 = 强弹屏, 用户是初炒者会加剧情绪化操作). 飞书消息**允许** (飞书是"打开才看", 不是强弹).

**静默期**:
- 用户在飞书标 "静默"后 24 小时内不发非紧急通知
- 但 PR-L 触发 / 卫星硬边界触发**永远发**

### 13.3 信号日常触达 (盘前 + 实时 + 收盘 三层)

**大白话**: 卫星 BUY 信号什么时候提醒你? 用**三层递进**——盘前一个摘要预告今天可能有啥, 盘中实时推关键信号 (但每天最多 5 条防轰炸), 收盘一个日总结. 全部走飞书, **不推手机 push**.

**层 1 — 盘前摘要 (每交易日 8:30)**

- 内容:
  - 今日预计的核心 ETF 再平衡信号 (若是月度触发日)
  - 昨日新产生但未处理的卫星 BUY 信号 (供 T+1 决策)
  - 卫星硬边界距离 (例: 60 天累计亏损 -2.1%, 距 5% 边界还有 2.9%)
  - PR-L 状态 (正常 / 触发中 / 部分解除)
- 空日也发 (只有一行"今日无新信号, 组合无异常"), 让你确认系统在跑
- 送达: 飞书 OPS 群

**层 2 — 盘中实时 (盘中 9:30-15:00, 严格限流)**

**默认关闭**, 只在明确开启后才推 (设置里开关). 开启后规则:
- **每交易日最多 5 条** (超过 5 条改为聚合成"过去 30 分钟内又有 3 条", 每 30 分钟一次)
- **只有以下类型才推**:
  - 卫星 BUY 信号已通过 EV gate (`gate_pass = true`)
  - 单笔止损触发 (深止损 -15% 或组合 PR-L)
  - 卫星硬边界警戒 (距离 <1% 时)
- **不推**:
  - 卫星信号被 EV gate 拒绝 (`gate_pass = false`) — 盘后再看
  - 核心 ETF 因子日常波动
  - Confidence 计算完成
- 每条消息带 UI 直链, 用户点开二次确认

**层 3 — 收盘日总结 (每交易日 15:30)**

- 今日新产生的所有信号 (通过 gate + 被拒绝的都列出)
- 今日已执行的交易 (若开自动化)
- 今日组合 PnL (核心 / 卫星分开显示)
- 明日预告 (若明天是月度再平衡日, 提前 1 天预热)

**告警优先级** (多条并存时的顺序):
1. PR-L 触发 (置顶, 永远发)
2. 卫星硬边界触发 (永远发)
3. 层 2 实时信号 (受限流)
4. 层 1 盘前 / 层 3 收盘 (定时)
5. 系统层告警 (cron / DB)

**触达渠道对照表**:

| 事件 | 层 1 盘前 | 层 2 实时 | 层 3 收盘 | 手机 push |
|---|---|---|---|---|
| 卫星 BUY 通过 EV | ○ (聚合) | ● (若开开关) | ● | ✗ |
| 卫星 BUY 被 EV 拒 | ✗ | ✗ | ● | ✗ |
| 单笔 -15% 止损 | ✗ | ● | ● | ✗ |
| 卫星硬边界警戒 | ● | ● (距 <1%) | ● | ✗ |
| 卫星硬边界触发 | 立即 | 立即 | 立即 | ✗ (飞书够) |
| PR-L 触发 | 立即 | 立即 | 立即 | ✗ (飞书置顶) |
| 核心 ETF 再平衡 | ● (触发前 1 天) | ● (触发时) | ● | ✗ |
| Override | ✗ | ● (立即, 审计) | ● | ✗ |
| Confidence 计算失败 | ○ (聚合) | ✗ | ● | ✗ |

**图例**: ● = 必推; ○ = 有内容才推; ✗ = 不推

### 13.4 数据回填

**大白话**: 系统要跑 ETF 因子和 confidence, 需要**历史数据**——过去几年每天每只 ETF 的价格 / 财务指标 / 每条历史信号的结果。这些以前没存, 现在得补上, 大概 3-4 天工作量, 严格按依赖顺序补, 补完做质量检查。

**背景**: Wave 1 建 ETF 因子需要历史因子数据, 建 confidence 服务需要历史 win% 数据, 两者都要 backfill (补历史)。

**回填任务清单**:

| 任务 | 数据 | 时间窗 | 工作量 | 前置 |
|---|---|---|---|---|
| **T1** | daily_bars for 50 只候选 ETF | 2022-01-01 至今 | 0.5 天 (已有 PR-N / 增量 sync) | 无 |
| **T2** | ETF 成分股财务因子 (PB/PE/ROE/股息率) | 每月 1 次快照, 2022 至今 | 1 天 | T1 |
| **T3** | 因子分历史 (Value/Quality/LowVol/Momentum for each ETF) | 从 T1+T2 计算, 每月 1 次快照 | 0.5 天 | T1, T2 |
| **T4** | `limit_up_stocks` 历史 (卫星 detector 依赖) | 2026-06-02 至今 (§6.2 提到) | 0.5 天 | 无 |
| **T5** | `theme_fermentation_phases` 历史 | 用 T4 + `industry_sentiment_indices` 计算 | 0.5 天 | T4 |
| **T6** | 历史信号 (`ai_investment_signals`) 迁移 | 保留 tradingagents 部分, 打 archive tag | 0.5 天 | 无 |
| **T7** | `recommendation_trade_outcomes` 历史 | 保留全部 (战略镜子 Q1 用) | 0 天 (已有) | 无 |

**总耗时**: 3-4 天 (与新代码开发并行)

**回填错误处理**:
- 数据源 API 失败 → 记录到 `data_update_logs` 表, 手动重试
- 部分数据缺失 (如某 ETF 2022 年数据不全) → 该 ETF 从候选池删除
- Backfill 期间 prod 服务**必须停** (auto_trade off), 避免半态数据触发错误信号
- 每步 backfill 完成后写一份**快照报告** (`docs/backfill/YYYY-MM-DD-taskX.md`) 记录: 数据量、缺失情况、决定

**Backfill 顺序** (严格依赖):
1. T1 (基础数据)
2. T4 (卫星基础数据, 与 T1 并行可)
3. T2 (依赖 T1)
4. T3 (依赖 T1 + T2)
5. T5 (依赖 T4)
6. T6/T7 (保留处理)

**回填质量门**:
- T1-T5 完成后, 跑一次**完整性检查**: 每个 ETF 至少有 90% 交易日数据, 因子分不为 null
- 完整性 < 90% 的 ETF 从候选池删除
- 完整性 90-99% 的 ETF 保留但标注"数据有缺失"

---

## 附录 Z — Plan A 因子数据链落地 (2026-07-03)

上文 §回填 的 T2/T3 (ETF 成分股财务因子 + 因子分) 由本节的具体脚本 + 调度实现。
**采用 Plan A(东方财富免费源)**:纯 TS HTTP 客户端,无需 Python/akshare/Tushare token,
零成本、开箱即用。仅 ETF→成分股映射(index_components)仍依赖 AKShare。

### Z.1 命脉数据链(为什么必须有)

Core 70% = ETF 因子轮动。`ETFFactorService` 打分读三张表:
- `stock_valuation_factors`(value)、`stock_fundamental_factors`(quality)、`stock_money_flow_factors`
- 经 `ETFConstituentExpander`(读 `index_components` / `fund_top_holdings`)把 ETF 展开成成分股做横截面 z-score

**任一环空 → `data_incomplete=true` → `total_score=-Infinity` → `ETFRankingService` 全过滤 → Core 腿空仓。**
故三张因子表 + 成分股表必须有稳定的定时写入源。重构前这些表全空、且写入方法无 CLI/无 cron。

### Z.2 新增脚本 / 调度

| 组件 | 类型 | 说明 |
|---|---|---|
| `backend/src/scripts/sync-derived-factors.ts` | CLI | 包装 `stockFactorService.syncDerivedFactors({provider:'eastmoney'})`,落三张因子表。`npm run sync:derived-factors` |
| `DERIVED_FACTOR_SYNC` | cron 任务 | 每交易日 **17:00**(早于 FACTOR_SCORE_COMPUTE 17:30),`provider=eastmoney limit=6000`,覆盖率≥95% 跳过重复落盘 |
| `INDEX_COMPONENT_SYNC` | cron 任务 | 每周一 **07:00** 同步 6 个宽基指数成分股(AKShare),供成分展开 |
| `sync-index-components.ts` | CLI(已存在) | `INDEX_COMPONENT_SYNC` 调用它;依赖 AKShare(Python) |
| `sync-extra-dims --dim=fund` | CLI(已存在) | `fund_top_holdings` 兜底映射,季报披露后手动/季度跑 |

均已登记到 `cronRegistry.ts` 白名单 + `ensureDefaultTasks` seed。`DERIVED_FACTOR_SYNC`
加入 catch-up 白名单(60min cooldown):**部署重启 / 错过 17:00 窗口会自动补跑**,满足"部署即开跑"。

另:`StockFactorService.resolveStocks` 全市场 cap 由 1000 提到 6000 —
否则宽基 ETF 成分缺口 >30% 触发 data_incomplete 硬门。

### Z.3 部署首刷顺序(在服务器上执行)

```bash
cd backend
# 1) 成分股(周度数据,首刷必须先跑;依赖 AKShare,需 PYTHON_PATH 指向装了 akshare 的解释器)
npm run sync:index-components -- --indexes=000300,000905,000016,000688,399673,000015 --date=$(date +%F)
# 2) 派生因子(命脉,东方财富免费源,~5500 票批量快照 5-20min)
npm run sync:derived-factors -- --provider=eastmoney --limit=6000
# 3) 因子打分
npm run compute:factors -- --date=$(date +%F)
# 4) 验证 Core 腿非空
#    跑 ETFRotationService.runMonthlyRebalance(dryRun) 或 ETF_FACTOR_ROTATION_REBALANCE 手动触发
#    确认 skipped_data_incomplete 不再等于全部候选、current_holdings 非空
```

之后由 scheduler 托管:`INDEX_COMPONENT_SYNC`(周一 07:00)→ `DERIVED_FACTOR_SYNC`(每日 17:00)
→ `FACTOR_SCORE_COMPUTE`(17:30)→ `FACTOR_IC_COMPUTE`(19:00)→ `ETF_FACTOR_ROTATION_REBALANCE`(每月首交易日 09:30)。

### Z.4 环境依赖

- **东方财富**:无 token,纯 HTTP(`push2.eastmoney.com`);需服务器能出网访问东财。本地 Mac 出网被限
  (socket hang up),故按用户要求在服务器跑。并发由 `EASTMONEY_FACTOR_CONCURRENCY`(默认 5)控制。
- **AKShare**(仅 index_components):`PYTHON_PATH` 指向装了 akshare 的解释器;未装则该任务失败但不影响
  eastmoney 因子链(宽基 ETF 可退回 `fund_top_holdings` 兜底)。
- **已知无关问题**:`sequelize.sync({alter:true})` 对 `northbound_holdings` enum 列生成非法 `USING` SQL,
  dev 模式建表会报错。生产用 `NODE_ENV=production`(跳过 alter)或既有 migration,不受影响。

## 附录 AA — 批9 调度器残留清理 + 计划闭环复核 (2026-07-03)

### AA.1 起因

复核"非本次范围"提法时发现: 批5 下线 5 个 service 时**只做了一半** —— 删了 service、
把 dispatch 改成空跑桩、cronRegistry 移除条目, 但**漏删 `ensureDefaultTasks` 里的 seed**,
导致 fresh DB / 存量 DB 仍把这些"已下线"任务当 active 每天空转。cron-registry 一致性单测
因此长期 1 failed(dispatch 有分支、registry 无条目)。这不是"历史漂移", 是下线未收尾。

### AA.2 清理批5下线残留 (commit 2a33a61)

| 动作 | 细节 |
|---|---|
| 删死 seed | `AI_DAILY_SCREENER`×4 / `AUTO_RECOMMENDATION_LOOP`×1 / `EARNINGS_FORECAST_WATCH`×2 / `STRATEGY_KILL_SWITCH_CHECK`×1 |
| 删死分支 | `全市场荐股闭环` 参数补丁 if 块(80行)+ `patchAutonomousPortfolioParameters` Set 项 |
| 保留 dispatch 空跑桩 | dispatch 链末尾兜底会 `throw Unsupported task type`; 桩是 DB 存量任务的安全网, 非脏物 |
| registry 转 retired | 5 个墓碑注释 → 正式 `retired: true` 条目(新增 `CronTaskDefinition.retired` 字段) |
| DB 迁移 | `2026-07-03-retire-batch5-offline-crons.sql`(+回滚): 按 type 幂等 disable 存量任务 |

### AA.3 计划闭环复核 — 发现对称的"上线做一半" (commit 8c07d7f)

用同一把尺子扫 registry↔seed↔dispatch 三方一致性, 发现 4 个 cron **从引入至今从未 seed**
(git 溯源确认), Batch AJ 补漏时又遗漏 → fresh DB / DR 重建后不会自动起跑:

| type | 引入 | 性质 |
|---|---|---|
| `ANALYST_FORECAST_SYNC` | BH-2 | 修 analyst_consensus 因子 std<0.02 真因 — **Signal-First 主线** |
| `SHAREHOLDER_COUNT_SYNC` | BH-3 | 修 shareholder_concentration 因子 std<0.10 真因 — **主线** |
| `DATA_FRESHNESS_CHECK` | BF-3 | 数据陈旧度告警 |
| `DAILY_HEALTH_REPORT` | BF-4 | 每日健康日报 |

已补 4 处 seed, cron_expression 对齐 cronRegistry recommendedCron。

### AA.4 最终自洽状态

- registry 81 个 = 活 77 个**全部 seed** + 5 个 retired 墓碑**不 seed**(正确)
- seed / dispatch / registry 三方完全对齐
- 验证: `tsc --noEmit` 0 错误; `cron-registry.test.ts` **757 ok / 0 failed**(原 1 failed)
- 本地 DB 迁移实测 `UPDATE 3` + 幂等复跑 `UPDATE 0`

### AA.5 元教训

"下线"和"上线"都必须走完 **dispatch + registry + seed + DB** 四点闭环, 缺一点就留尾巴:
下线漏删 seed → 死任务空转; 上线漏加 seed → 活能力 fresh DB 起不来。
cron-registry 双向一致性单测只守 dispatch↔registry 两点, seed 一致性靠本次复核补上。

## 附录 AB — 批9 补漏#2 + 89 处下线标记全量定性 (2026-07-03)

### AB.1 起因
附录 AA 用"下线/上线做一半"这把尺子扫完 cron 四点后, 继续对全后端 `已下线 / 空跑 / DEPRECATED / 已废弃 / 已移除` 标记 (89 处 / 27 文件) 逐类定性, 排查是否还有同型尾巴。

### AB.2 发现并收尾: 第 6 个"下线做一半"
**`推荐信号模拟盘跟单` cron** (PAPER_TRADING_AUTO_SYNC, `40 15 * * 1-5`, DB id=7):
- `refresh_recommendations=true` + `source_type='quant_recommendation'`。
- 批5 已删 `QuantRecommendationService` — 全系统再无服务产出 `quant_recommendation` 信号。
- 后果: `runAutoSync` 的 `refreshRecommendations` 分支恒归档 0 条; `autoBuyFromSignals` 亦查不到新信号 → **每交易日 15:40 纯空跑**。
- 与批9 清理的 5 个批5 下线 cron 同型 (service 删了 / seed 没删), 属遗漏的**第 6 个**。

**收尾 (commit 7b77a00)**:

| 动作 | 位置 |
|---|---|
| 删空跑 seed 块 | `SchedulerService.ensureDefaultTasks` |
| 空候选分支补"批5/批9 定性"注释 (保留兼容 UI/autonomous HTTP 入口) | `PaperTradingAutomationService.runAutoSync` |
| DB 存量置 inactive (幂等 by name+type) + rollback | `migrations/2026-07-03-retire-quant-rec-paper-sync.sql` |

> 保留 `refreshRecommendations` 分支 (未删): HTTP `auto_sync` / `autonomous_auto_sync` 入口仍在, 删分支会抛错。分支已恒空跑并注释说明。

### AB.3 关键定性: Core 70% paper 执行 = 出口 B (后期), 不在本次范围
- ETF 轮动信号: `decision=BUY, normalized_decision=BUY`, 但 `action='TARGET_WEIGHT'` (再平衡语义, 非 `'buy'` 动作)。
- `autoBuyFromSignals` 粗筛只放行 `metadata.action==='buy'` → **ETF 轮动信号即使被跟单 cron 拾取也会被 action 粗筛全拒**。
- 结论: Core 70% 的 paper 自动执行需**独立 TARGET_WEIGHT 再平衡执行路径**, 不复用 quant_recommendation buy 跟单链。按计划 §层2, 自动执行 = **出口 B (后期)**; 当前主交付物是**信号** (出口 A: ETFRotationService 落信号 → V3 展示 → 用户拍板), 已闭环。故此项非收尾尾巴, 属计划既定的后期能力。

### AB.4 其余 87 处标记定性 (全部已闭环, 无尾巴)
| 类别 | 数量级 | 定性 |
|---|---|---|
| 批8 删表 DEPRECATED STUB (恒返回 `[]`/`null`/`0`) | ~13 文件 | 优雅降级墓碑; 消费方全在 backtest / performance / health / report **侧支**, 非 Core-Satellite 交易决策链。有横幅文档。 |
| 批5 service 下线的调用点中性化 (`SourceTypeWinRateAdjuster` / `KOLAggregatorService` / `StrategyKellyStatsService` / `RecommendationLoopPolicySnapshot` / `BudgetPolicyVersionSnapshot` / `FieldGateAdjustmentAttribution` 等) | ~10 处 | service 已删, 调用点返回中性值 / null; grep 确认无任何活代码 `new`/调用, 仅剩注释。已闭环。 |
| HTTP 410 端点 (`earnings-forecast` / `strategy-copilot` / `kol` 等) | ~7 处 | 能力下线后端点显式返 410; 契约明确。 |
| 其它 (`train-meta-label-v2` 版本串 / `BacktestEngine` events 省内存 / `check-l8-activation` 硬编码移除 evidence / `tradingCalendar` 回退注释) | ~5 处 | 非下线残留, 是正常实现注释。 |

### AB.5 最终状态
- `tsc --noEmit` = 0 err; `cron-registry.test.ts` = 757 ok / 0 failed。
- DB: 6 个批5 下线 cron 全部 inactive (5 个批9 + 1 个批9#2)。
- 全后端下线标记逐类定性完毕, **除出口 B (计划既定后期能力) 外无遗留尾巴**。计划闭环复核结束。
