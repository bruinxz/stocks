# Ralph 200 轮启动指南（凌晨自动推进中）

**启动时间**：2026-06-19 02:24
**Ralph PID**：82287（200 轮，每轮 1 story）
**Branch**：`ralph/trader-system-prod`
**PRD**：[ralph/prd.json](../../ralph/prd.json) — 147 user story (P0=94)
**设计文档**：[docs/trader-system/](../trader-system/) 54 份
**目标**：让"高级操盘手"系统全自动赚钱（sharpe ≥ 1.5 / 30 日 dd ≤ 12% / 每笔 ≥ 3 evidence）

---

## 当前后台进程

| PID | 进程 | 角色 |
|---|---|---|
| 82287 | `ralph.sh --tool claude 200` | 主循环，每轮启动一个 claude 实例做一个 story |
| 83591 | `ralph-watchdog.sh` | 20 min 没 log 更新就 kill claude 让主循环进下一轮 |
| 83613 | `ralph-meta-watchdog.sh` | 看 watchdog 是否还活着，挂了重启 watchdog |
| 83620 | `ralph-resumer.sh` | 5 min 检查 ralph 是否退出，若还有未完成 story 自动续 20 轮 |

---

## 早上起来怎么看进度

### 1. 看完成多少 story
```bash
cd /Users/bytedance/go/src/github.com/bruinxz/stocks/ralph
jq '[.userStories[] | select(.passes == true)] | length' prd.json
jq '[.userStories[] | select(.passes == false)] | length' prd.json
```

### 2. 看 ralph 现在第几轮
```bash
tail -30 /Users/bytedance/go/src/github.com/bruinxz/stocks/ralph/ralph-run.log | grep -E "Iteration|Story"
```

### 3. 看最近 commit
```bash
cd /Users/bytedance/go/src/github.com/bruinxz/stocks
git log --oneline -30
```

### 4. 看 progress.txt（每个 story 的 learning）
```bash
tail -200 /Users/bytedance/go/src/github.com/bruinxz/stocks/ralph/progress.txt
```

### 5. 看 watchdog 是否还活
```bash
pgrep -laf "ralph-"
ls -la /Users/bytedance/go/src/github.com/bruinxz/stocks/ralph/*.heartbeat
```

---

## 如果发现卡住了

### Ralph 主循环挂了
```bash
cd /Users/bytedance/go/src/github.com/bruinxz/stocks/ralph
nohup ./ralph.sh --tool claude 200 > ralph-run.log 2>&1 &
```

### Watchdog 全挂了
```bash
cd /Users/bytedance/go/src/github.com/bruinxz/stocks/ralph
nohup ./ralph-watchdog.sh > ralph-watchdog.log 2>&1 &
nohup ./ralph-meta-watchdog.sh > ralph-meta-watchdog.log 2>&1 &
nohup ./ralph-resumer.sh > ralph-resumer.log 2>&1 &
```

### 想终止全部
```bash
pkill -f "ralph.sh --tool claude"
pkill -f "ralph-watchdog.sh"
pkill -f "ralph-meta-watchdog.sh"
pkill -f "ralph-resumer.sh"
```

---

## 跳过的 story（需要你决定）

Ralph 遇到以下情况会**跳过 + 写 notes**，不会自动做：
- 触实盘 API / 真实下单
- 修改 `.env` / bridge secrets
- 修改 `docker-compose.yml` / 部署脚本
- 修改 git 历史

早上 grep notes 看哪些被跳过：
```bash
jq -r '.userStories[] | select(.passes == false and (.notes // "") != "") | "\(.id) \(.title) — \(.notes)"' /Users/bytedance/go/src/github.com/bruinxz/stocks/ralph/prd.json
```

---

## 设计文档全清单（54 份）

`docs/trader-system/` 下：
- `00_overview.md` — 操盘手方法论 + 6 大模块
- `01-12` — 数据层 12 份
- `20-23` — Alpha 引擎 4 份
- `30-38` — 策略层 9 份
- `40-43` — 组合执行 4 份
- `50-55` — 风控 5 份
- `60-64` — 执行 + 对账 4 份
- `70-76` — 复盘 7 份
- `80-84` — AI 5 份
- `90-91` — 前端 2 份
- `99_implementation_roadmap.md` — 路线图（210 故事汇总）

---

## 预期

按当前 ralph 速度（1-2 story/小时），200 轮约：
- 完成 P0 (94 story) ≈ 7-10 天连续推进
- 全部 147 story ≈ 15-25 天

中途遇到的问题 ralph 会自己想办法解决；watchdog 兜底防卡死；resumer 兜底防退出。

**红线**：触实盘 / secrets / 部署 → 跳过，等你决定。
