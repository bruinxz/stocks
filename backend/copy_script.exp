#!/usr/bin/expect -f
set timeout -1
set password [lindex $argv 0]

spawn scp -o StrictHostKeyChecking=no -P 14126 fix_db.ts root@103.242.3.87:/opt/stocks/backend/fix_db.ts
expect {
    "*yes/no*" { send "yes\r"; exp_continue }
    "*assword:*" { send "$password\r"; exp_continue }
    eof
}
